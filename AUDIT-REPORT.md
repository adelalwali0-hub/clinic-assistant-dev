# تقرير Architecture Audit — Beauty Clinic Revenue Growth Engine

**النطاق:** قراءة وتتبع فقط. لم يُعدَّل/يُنشأ/يُحذف أي ملف. لم يُكتب أي كود.
**المراجع:** `PRD.md` + `PRD-AMENDMENTS.md` (الملحق مُقدَّم عند التعارض) للتعريفات · الكود مرجع الواقع.
> **ملاحظة لاحقة (لا تعديل على نتائج التقرير):** دُمج الملحق في `PRD.md` v3.2 وحُذف ملفه. ما قرأه هذا التقرير وقت كتابته هو `PRD.md` v3 + الملحق؛ محتواهما اليوم في `PRD.md` وحده.
**حالة البيئة:** لا `data/`، لا `leads.csv`، لا ملفات اختبار — مستودع نظيف.

**ملاحظة تعارض `AGENTS.md`:** ما زال يصف المشروع «نظام مساعد محادثة للعيادات يركز على Telegram» (السطر 3) — يتعارض مع تموضع `PRD.md §1/§2` («لا نبني چات بوت»، «المحادثة قناة تنفيذ»). عومل كوثيقة تاريخية غير حاكمة. كذلك `.memory/decisions.md` D-005 و`.memory/glossary.md` يذكران أن «GPT يُستدعى في كل رسالة حتى عند `USE_AI_INTENT=false`» — **الكود يخالف ذلك**: `main.py:44‑48` يرجع قبل أي استدعاء AI. الكود هو الحقيقة.

---

## القسم 1 — تتبع المسار الحي

المسار الفعلي (لا يوجد `storage/session_store.py → leads_store.py → telegram_channel.py` بهذا التسلسل؛ التسلسل الحقيقي):
`telegram_channel._poll_loop` → `MessageRouter._on_message` → `combined_handler` (main.py) → `business_logic.handle_message/_decide` → (`session_store` و/أو `leads_store`) → `reply_text` يعود إلى Router → `channel.send_message`.

جميع السيناريوهات نُفِّذت بـ`USE_AI_INTENT=false` (الوضع المرجعي في `PRD §17`).

### سيناريو ١ — "شكد سعر البوتوكس؟" ثم "نعم" ثم "سارة 0770..."

| الرسالة | الحالة المخزّنة | ما كُتب على القرص | ما أُرسل |
|---|---|---|---|
| «شكد سعر البوتوكس؟» | `session_store`: `{state: awaiting_booking_confirmation, service: {name:"حقن البوتوكس", keywords:[...], price:"120,000 دينار"}}` | `data/sessions.json` فقط (كتابة ذرية). **لا شيء في `leads.csv`.** | «سعر حقن البوتوكس في Luna Beauty Center هو 120,000 دينار. هل ترغبين بحجز موعد؟» (`business_logic.py:126‑130`) |
| «نعم» | `state → awaiting_contact_info`، الخدمة تبقى | `data/sessions.json`. لا شيء في `leads.csv`. | «ممتاز! الرجاء إرسال اسمك ورقم هاتفك في رسالة واحدة لتأكيد الحجز.» (`:111`) |
| «سارة 0770...» | `clear_session` → `{state: idle, service: None}` | **`leads.csv`**: صف جديد `الحالة="confirmed"`، `بيانات التواصل="سارة 0770..."` (النص كما هو، **بلا أي تحقق/تحليل**)، `سعر الخدمة وقت الإنشاء` = نتيجة `_lookup_current_price("حقن البوتوكس")` **لحظة حفظ الصف** لا لحظة عرض السعر (`:225`)، `مرحلة المتابعة="0"`، `نتيجة المتابعة=""`. | «شكراً لك 🌸 تم تسجيل حجزك لخدمة حقن البوتوكس...» (`:86‑89`) |

**نقطة حرجة:** `clear_session` (`:77`) تُنفَّذ **قبل** `save_lead` (`:78`). انقطاع بينهما = جلسة نُظّفت + لا Lead = حجز ضائع بلا أثر.
**تصادم دلالي:** الحالة تُكتب حرفياً `"confirmed"` عند استلام الهاتف (= `Booking Request` في `PRD §8`)، لا عند تأكيد الموظفة (`Confirmed Booking`). → F2.

### سيناريو ٢ — "شكد سعر البوتوكس؟" ثم "لا"

- الرسالة ١: كما أعلاه — جلسة `awaiting_booking_confirmation`، **لا صف في `leads.csv`**.
- «لا»: `CONFIRM_WORDS` تُفحص أولاً (`:95`) — لا تطابق. `DECLINE_WORDS` تحوي `"لا"` (`:40`) → `rule_branch="decline"` → `clear_session` ثم `save_lead(status="not_ready")` (`:113‑115`). الصف: `الحالة="not_ready"`, `بيانات التواصل=""`, `سعر=120,000`, `مرحلة="0"`, `نتيجة=""`.
- أُرسل: «تمام 🌸 إذا احتجتِ أي معلومة إضافية عن خدماتنا أنا موجودة.» (`:116`)
- **النتيجة:** هذا الـLead الوحيد الذي يدخل دورة المتابعة — أي **الرافضة صراحةً تُتابَع، والصامتة لا** (عكس المنطق التجاري في `PRD §7`).

### سيناريو ٣ — "شكد سعر البوتوكس؟" ثم لا شيء

- يُكتب فقط: `data/sessions.json` = `{state: awaiting_booking_confirmation, service: botox}`. **صفر في `leads.csv`.**
- **بعد أسبوع:**
  - **`leads.csv`:** لا صف إطلاقاً. `save_lead` لا تُستدعى إلا في فرعَي `awaiting_contact_info` و`decline` (`business_logic.py:78, 115`). → **F1 مؤكَّدة.**
  - **دورة المتابعة:** لا. `get_leads_eligible_for_first_followup` (`leads_store.py:234`) تقرأ `leads.csv` وتطلب `الحالة == "not_ready"`. لا صف ⇒ لا متابعة أبداً.
  - **الجلسة:** عالقة في `awaiting_booking_confirmation` **إلى الأبد** — لا TTL، لا تنظيف مجدول، لا حد زمني في أي مكان.
  - **رسالة جديدة لاحقاً:** تُعامَل كرد على السؤال القديم. مثال: ترسل «شكد سعر الفيلر؟» بعد أسبوع → الحالة `awaiting_booking_confirmation` → تُفحص ضد `CONFIRM/DECLINE/HESITANT` → لا تطابق → فرع `other` → «هل ترغبين بتأكيد حجز موعد؟ (نعم / لا)» (`:121`). **استفسارها الجديد ضاع، ولا يمكنها الخروج من الحلقة** إلا بكلمة تحوي `نعم/لا` كسلسلة فرعية. لو كتبت لاحقاً «نعم» (لأي سبب) → تحجز **البوتوكس بالسعر القديم**.

### سيناريو ٤ — "شكد سعر الليزر؟" مع أكثر من خدمة ليزر

`config` الحالي فيه خدمة ليزر واحدة (`keywords:["ليزر", ...]`). **مع تعدد** (ليزر وجه/جسم/بكيني):
`find_service` (`services.py:82‑89`) تكرّر `SERVICES` **بترتيب الملف** وتُرجع **أول خدمة** تظهر إحدى كلماتها المفتاحية كسلسلة فرعية. «ليزر» ستطابق أول خدمة عنوانها/كلماتها تحوي «ليزر» → **يُرسل سعر الليزر الأول في القائمة** بغضّ النظر عن مقصد العميلة. لا مفهوم «تطابق متعدد»، لا سؤال استيضاح. `normalize_arabic` تجرّد «الليزر» → «ليزر» فيتوسّع التطابق. → **F6 / خرق S2.**

---

## القسم 2 — الفجوات الثماني

| # | الحكم | الدليل | أخطر مما وُصف؟ |
|---|---|---|---|
| **F1** | **مؤكَّدة** | `save_lead` تُستدعى حصراً في `business_logic.py:78` (contact) و`:115` (decline). الصمت وفرع `hesitant` (`:118`) لا ينشئان أي سجل. | **نعم.** حتى «متردّدة» لا تُسجَّل. وفي سيناريو ٣ الجلسة العالقة تحوّل كل رسالة لاحقة لرد على سؤال ميت. |
| **F2** | **مؤكَّدة** | `status="confirmed"` تُمرَّر في `:79` لحظة استلام نص الهاتف. `PRD §8`: `Confirmed Booking` = «الموظفة أكّدت الموعد». | نعم — الاسم `confirmed` يوحي بالطبقة الثالثة بينما هو الطبقة الأولى (`Booking Request`)، فيُقرأ خطأً في كل تقرير ومعادلة. |
| **F3** | **مؤكَّدة** | `compute_recovery_metrics` (`leads_store.py:334‑338`): `revenue = Σ سعر` لصفوف `نتيجة المتابعة=="مسترجَع"`. صف يصير «مسترجَع» في `:216` لحظة `save_lead(confirmed)` = لحظة تسليم الهاتف. **لا حضور في النظام إطلاقاً**، والملحق §4 يؤكد أن بيانات الحضور **خارج وصولنا**. | نعم — لا يوجد حتى مسار مستقبلي لتصحيحه ذاتياً؛ يحتاج لمسة بشرية غير مبنية. |
| **F4** | **مؤكَّدة** | لا `events`، لا `.jsonl`، لا جدول أحداث في المستودع. كل الأثر `print()` إلى stdout (`message_router.py:82`, `send_followups.py:68`) — يزول بانتهاء العملية. | نعم — حتى `success/FAILED` من `send_message` يُطبع ويُهمَل، فلا نعرف إن وصلت رسالة. |
| **F5** | **مؤكَّدة** | `send_followups.py:22‑23` يستورد `TelegramChannel` و`OutgoingMessage` مباشرة، ويستدعي `channel.send_message` في `:64`, `:86` دون المرور بـ`MessageRouter`. Idempotency والـFallback في Router غير مطبَّقين على المتابعات. | كما وُصف. مساران، صفر قواعد مشتركة. |
| **F6** | **مؤكَّدة** | `find_service` تُرجع أول تطابق (`services.py:88`)؛ لا عدّ للمطابقات. `PRD §18 S2` = Pilot Blocker. | **نعم.** نفس أسلوب السلسلة الفرعية يكسر `DECLINE_WORDS` أيضاً: `"لا"` سلسلة فرعية في «يلا نحجز»، «تعالي»… → رفض كاذب + `save_lead(not_ready)`. وكلمة `"yes"`/«اوك» قد تطابق داخل كلمات أطول. |
| **F7** | **مؤكَّدة** | لا عمود `lead_id`، لا توليد معرّف. الهوية = `message.user_id` = `chat.id` (`telegram_channel.py:82`). مفتاح عمليات المتابعة = `(التاريخ والوقت النصّي, معرف العميل, الخدمة)` في `mark_followup_sent` (`leads_store.py:302‑306`). | نعم — استفساران في نفس الثانية لنفس الخدمة/العميل ⇒ تصادم صامت؛ `mark_followup_sent` يُحدّث أول تطابق فقط (`break` في `:310`). |
| **F8** | **مؤكَّدة** | صفر ملفات `test_*` / `conftest` / `pytest.ini`. | كما وُصف. |

### الفجوة التاسعة (بنفس الخطورة، غير مذكورة)

**F9 — لا دورة حياة للجلسة: الجلسات لا تنتهي، و`awaiting_contact_info` تبتلع أي نص كـ«بيانات تواصل» وتُنشئ حجزاً.**

الدليل: `business_logic.py:75‑89` — أي رسالة تصل والجلسة `awaiting_contact_info` تُؤخذ حرفياً كـ`contact_info` وتُنشئ صف `confirmed`. لا تحقق أنها هاتف، لا مهلة. أمثلة:
- عميلة في `awaiting_contact_info` ترسل «شنو يعني بالضبط؟» → صف حجز `confirmed` ببيانات تواصل = «شنو يعني بالضبط؟».
- عميلة صامتة (سيناريو ٣) عالقة في `awaiting_booking_confirmation` للأبد؛ كل استفسار جديد يُبتلَع.
- `session_store` ليس فيه أي TTL أو تنظيف؛ `data/sessions.json` ينمو بلا حد ويُبقي حالات ميتة.

هذه **أخطر من F1 في وجه واحد**: F1 يفقد بيانات، F9 **يُلفّق** بيانات حجز خاطئة داخل `leads.csv` — الملف نفسه الذي يفترض Gate A أن يجعله جديراً بالثقة. تصنيفها: 🔴.

---

## القسم 3 — دورة حياة الـLead كما ينفّذها الكود

### الحالات الفعلية ومخازنها

| الحالة الفعلية | أين تُخزَّن | مرئية في `leads.csv`؟ |
|---|---|---|
| لم تتواصل / بعد `clear_session` | `session_store`: `idle` | لا |
| سُعِّرت، تنتظر جواب الحجز | `session_store`: `awaiting_booking_confirmation` + الخدمة | **لا (F1)** |
| وافقت، تنتظر بياناتها | `session_store`: `awaiting_contact_info` | **لا (F1)** |
| رفضت صراحة | `leads.csv`: `الحالة=not_ready, مرحلة=0, نتيجة=""` | نعم |
| متردّدة («خلي أفكر») | لا شيء — الجلسة تبقى `awaiting_booking_confirmation` | لا |
| بعد Follow-up 1 | `leads.csv`: `مرحلة=1, تاريخ آخر متابعة=<ts>` | نعم |
| بعد Follow-up 2 | `leads.csv`: `مرحلة=2` | نعم |
| منتهية | `leads.csv`: `نتيجة="منتهي"` (`mark_expired`) | نعم — نهائية |
| حجزت بعد متابعة | صف `not_ready` القديم: `نتيجة="مسترجَع"` + **صف `confirmed` جديد** | نعم — نهائية |
| حجزت قبل أي متابعة | صف `not_ready` القديم: `نتيجة="أُغلق"` + صف `confirmed` جديد | نعم |
| حجزت مباشرة (بلا `not_ready` سابق) | صف `confirmed` واحد، `نتيجة=""` | نعم |

### الانتقالات ومن يملكها

- `idle → awaiting_booking_confirmation`: `find_service` ينجح (`business_logic.py:124`). المالك: القاعدة.
- `awaiting_booking_confirmation → awaiting_contact_info`: كلمة تأكيد **أو نية AI `confirm_booking`** (`:104‑110`). **المالك: القاعدة أو الـAI** — راجع القسم 8.
- `awaiting_booking_confirmation → idle + Lead(not_ready)`: كلمة رفض أو نية AI `decline` (`:113`).
- `awaiting_contact_info → idle + Lead(confirmed)`: **أي رسالة** (`:75`).
- `not_ready → مرحلة 1 → مرحلة 2`: `send_followups.py` عند نجاح `send_message` **ثم** `mark_followup_sent`.
- `مرحلة 2 → "منتهي"`: `run_expire_pass`.
- `not_ready(نتيجة="") → "مسترجَع"/"أُغلق"`: تأثير جانبي داخل `save_lead(status="confirmed")` (`leads_store.py:207‑217`) — يطابق **أي** صف `not_ready` بنفس `(معرف العميل, الخدمة)` و`نتيجة==""`، حتى لو كان عمره أشهر.

### حالات نهائية
`"منتهي"` · `"مسترجَع"` · `"أُغلق"`. صف `confirmed` بلا آلية انتقال لاحقة (لا `Confirmed/Completed/No-show/Lapsed`).

### حالات العلوق الأبدي
1. جلسة `awaiting_booking_confirmation` / `awaiting_contact_info` بلا رسالة تالية — أبداً (لا TTL).
2. صف `not_ready` مرحلة 0 و`send_followups.py` لا يُشغَّل (يدوي، غير مدمج — D-013) — أبداً.
3. صف `not_ready` مرحلة 1، شُغِّل السكربت مرة ثم توقّف — عالق في مرحلة 1.
4. `send_message` ينجح لكن `mark_followup_sent` يُرجع `False` (لا تطابق للمفتاح الثلاثي) — يُطبع `[SENT]` والمرحلة لا تتقدم → **إعادة إرسال في كل تشغيل لاحق بلا حد**.

### تناقض بين `leads.csv` و`sessions.json`
ممكن: تحجز البوتوكس (صف `confirmed`، الجلسة `idle`)، ثم تسأل عن البوتوكس مجدداً → جلسة `awaiting_booking_confirmation` جديدة بينما `leads.csv` فيه صف `confirmed` سابق. بلا `lead_id` لا يمكن للنظام تمييز أنهما نيتان مختلفتان، وقد يطابقها التأثير الجانبي «مسترجَع» بصف قديم خاطئ. أيضاً نافذة الانقطاع بين `clear_session` و`save_lead` تُنتج: جلسة `idle` + لا Lead (فقدان)، وهو تناقض مع ما جرى فعلياً.

### الانحراف عن `PRD §7`
- لا `UNBOOKED` بعد نافذة صمت — الصمت لا يُنتج أي حالة (F1). `not_ready` ≠ `UNBOOKED` (الأول رفض صريح فقط).
- لا `holdout` — لا حقل، لا استثناء في محرك المتابعة (`PRD §10`).
- لا `DECLINED` كحدث — رفض = `not_ready` = نفس مسار المتابعة، أي **المرفوضة تُتابَع**.
- دورة الحجز (`REQUESTED → CONFIRMED → COMPLETED | NO_SHOW | LAPSED`) **غير موجودة إطلاقاً** — النظام يتوقف عند صف `confirmed` المسمّى خطأً.
- `AMBIGUOUS_SERVICE → استيضاح` غير موجود (F6).

---

## القسم 4 — الدلالات: هل يعني الكود ما تعنيه الوثيقة؟

| مصطلح الكود | موقعه | تعريف `PRD §8` | الحكم | الرقم الخاطئ أمام عيادة حقيقية |
|---|---|---|---|---|
| `status="confirmed"` | `business_logic.py:79`, `leads_store.py` | `Confirmed Booking` = الموظفة أكّدت | **مضلِّل** | «عدد الحجوزات المؤكَّدة» يُعرَض مساوياً لعدد من سلّمن هواتفهن — مضخّم بفجوة (تأكيد الموظفة × الحضور). عيادة تظن أن X تأكّدن بينما قد يحضر 0.5X. |
| `status="not_ready"` | `:115` | `Unbooked Lead` = Qualified لم يصل `BOOKING_REQUESTED` خلال نافذة الصمت | **مختلف** | يشمل فقط من قالت «لا» صراحةً. الأغلبية الصامتة غير محتسبة ⇒ «Unbooked» يُقلَّل بشدة ⇒ `Recovery Rate` (مسترجَع ÷ unbooked) **مضخَّم** لأن المقام منقوص. |
| `نتيجة="مسترجَع"` | `leads_store.py:216` | `Recovered Lead` = Unbooked تلقّى متابعة ثم أنتج `Booking Request` | **مطابق تقريباً** | سليم دلالياً، لكن الشرط «تلقّى متابعة» يعتمد على `مرحلة ∈ (1,2)` وقت الحجز — والملحق §2 (L6) يقول إن `مرحلة` تُكتب لحظة الاقتراح لا الإرسال الفعلي (في Concierge). |
| `نتيجة="أُغلق"` | `:216` | `organic` attribution (§9.1) | **مضلِّل بالاسم** | «أُغلق» يوحي بـ«فرصة خاسرة/مغلقة»؛ فعلياً = «حجزت عضوياً بلا فضل للمتابعة». تقرير يعدّها كخسارة سيقلب الاتجاه. |
| `نتيجة="منتهي"` | `mark_expired` | `EXPIRED` (§7) | **مطابق** | — |
| `مرحلة المتابعة` (0/1/2) | `leads_store.py` | عدّاد المتابعات المُرسَلة | **مطابق شكلاً / هشّ** | يتقدّم فقط عند نجاح `send_message` **ثم** `mark`. فجوة بينهما ⇒ رسالة أُرسلت والعدّاد 0 ⇒ إعادة إرسال. لا حدث `FOLLOWUP_SENT`. |
| `سعر الخدمة وقت الإنشاء` | `:225` (`_lookup_current_price` وقت `save_lead`) | «سعر وقت الاستفسار كصورة» (L4) | **مضلِّل** | يُلتقط لحظة **كتابة الصف** لا لحظة عرض السعر. لصف `confirmed`: العرض في الرسالة 1، الكتابة عند الرسالة 3 (قد تكون بعد أيام). لو تغيّر السعر في `config` بينها ⇒ الصورة المخزّنة = السعر الجديد، لا ما رأته العميلة. `Potential/Requested Revenue` يُحسب على رقم لم يُقتبَس فعلاً. |
| `compute_recovery_metrics().revenue_recovered` | `leads_store.py:336` | `Revenue` = Σ سعر `Completed` فقط (§8، قاعدة 🔴) | **مضلِّل — أخطر بند** | يجمع أسعار صفوف «مسترجَع» = `Requested Revenue` (بل أبكر: لحظة تسليم الهاتف). يُسمّى «Revenue Recovered» في `lead_recovery_report.py:16`. **هذا الرقم يُفوتَر عليه** (§9.3) وهو مضخَّم بكامل فجوة التأكيد + الحضور. |
| `bookings_recovered` | `:337` | `Recovered Completed Booking` (وحدة الفوترة) | **مضلِّل** | `= len(recovered_rows)` نفسها تماماً مثل `leads_recovered` — رقمان بنفس القيمة، أحدهما يوحي بحجوزات مكتملة. |
| `session state: idle` | `session_store.py:25` | — (خارج مفردات §8) | **مقبول داخلياً** | — |
| `awaiting_booking_confirmation` | `business_logic.py:91` | يقابل `PRICE_QUOTED` (بانتظار ردّها) | **مضلِّل بالاسم** | «booking_confirmation» يتصادم لفظياً مع `Confirmed Booking` (الموظفة). قارئ الكود يظن النظام ينتظر تأكيد الموظفة. |
| `awaiting_contact_info` | `:75` | جزء من الانتقال إلى `BOOKING_REQUESTED` | **مقبول** لكن الفرع نفسه معطوب (F9) | — |

---

## القسم 5 — تحديد الخدمة والغموض (Safety Rail S2)

`find_service` (`services.py:82‑89`) + `normalize_arabic` (`:67‑79`):

**ترتيب المطابقة:** حلقة مزدوجة — لكل خدمة بترتيب `config`، لكل كلمة مفتاحية بترتيبها؛ **أول** `normalize_arabic(kw) in normalized_text` يُرجع الخدمة فوراً (`:88`). عند تطابق أكثر من خدمة: **تفوز الأولى في ترتيب الملف**، بصمت.

**أثر `normalize_arabic`:**
- «الليزر» → إزالة «ال» → «ليزر» (يطابق). «بالليزر» → «با» ليست «ال» → تبقى، لكن «ليزر» سلسلة فرعية داخلها فتطابق أيضاً.
- «إزالة» → توحيد الهمزة → «ازاله» (بسبب `ة→ه`). الكلمة المفتاحية «إزالة شعر» → «ازاله شعر» **متلاصقة** — تفشل إن لم تُذكر «شعر» ملاصقة.
- «البشرة» → «بشره». المفتاح «تنظيف بشرة» → «تنظيف بشره» متلاصق.
- التجريد يقتصر على «ال» في بداية الكلمة وطولها > 2؛ لا يعالج «لل»، «بال»، «وال» بثبات.

**أمثلة لهجة عراقية تُنتج خطأً:**
| رسالة | النتيجة | السبب |
|---|---|---|
| «شكد الفلر؟» (بلا ياء) | صفر تطابق → قائمة الخدمات | «فيلر» ليست سلسلة فرعية من «فلر» |
| «بوتكس بكم؟» (بلا واو) | صفر تطابق | «بوتوكس» ≠ «بوتكس» |
| «بشرتي دهنية شنو تنصحون» | صفر تطابق | لا كلمة «تنظيف» حرفياً |
| «أريد ليزر بكيني» + config فيه «ليزر وجه» أولاً | **سعر ليزر الوجه** | أول تطابق لـ«ليزر» |
| «عندكم ليزر وتقشير؟ شكد الاثنين» | سعر الليزر فقط (الأسبق في الملف) | لا كشف تعدد |
| «يلا نحجز ليزر» (والجلسة `awaiting_booking_confirmation`) | **رفض** + `save_lead(not_ready)` | «لا» سلسلة فرعية في «يلا» تُفحص قبل أي شيء |

**مسار إرسال سعر خاطئ:** نعم، عدة:
1. تعدد خدمات بكلمة مفتاحية مشتركة → سعر الأولى.
2. كلمة مفتاحية عامة («ليزر») تطابق داخل كلمة أطول غير مقصودة.
3. تداخل كلمات مفتاحية بين خدمتين («تنظيف» في «تنظيف بشرة» و«تنظيف أسنان» لو أُضيفت).

**التصنيف:** S2 مكسورة — **Pilot Blocker 🔴**، وليست تحسين جودة.

---

## القسم 6 — الرسائل الصادرة وقابلية تتبّع الـVariants

### كل نص رد ممكن وموقعه الحرفي

| # | النص | الموقع | المسار |
|---|---|---|---|
| 1 | «سعر {name} في {CENTER} هو {price}.\nهل ترغبين بحجز موعد؟» | `business_logic.py:126‑129` | حي |
| 2 | «أهلاً بك في {CENTER} 🌸\nهذه خدماتنا المتوفرة حالياً:\n{list}\n\nأي خدمة تودين الاستفسار عن سعرها؟» | `:132‑136` | حي |
| 3 | «ممتاز! الرجاء إرسال اسمك ورقم هاتفك في رسالة واحدة لتأكيد الحجز.» | `:111` | حي |
| 4 | «تمام 🌸 إذا احتجتِ أي معلومة إضافية عن خدماتنا أنا موجودة.» | `:116` | حي |
| 5 | `HESITANT_REPLY` («تمام 🌸 خذي وقتك...») | `:47‑50` | حي |
| 6 | «هل ترغبين بتأكيد حجز موعد؟ (نعم / لا)» | `:121` | حي |
| 7 | «شكراً لك 🌸 تم تسجيل حجزك لخدمة {name} في {CENTER}.\nسيتواصل معك فريقنا...» | `:86‑89` | حي |
| 8 | `FALLBACK_ERROR_REPLY` («عذراً، صار خطأ بسيط 🙏...») | `message_router.py:36` | حي (استثناء) |
| 9 | `stub_business_logic` («وصلتني رسالتك...») | `message_router.py:29‑33` | **كود ميت** (`main.py` يمرّر `combined_handler`) |
| 10 | «مرحباً 🌸 لاحظنا أنك استفسرتِ سابقاً عن {service}{price}...» | `send_followups.py:34‑40` | متابعة |
| 11 | «🌸 آخر تذكير بخصوص {service} — إذا حابة تحجزين...» | `send_followups.py:43‑47` | متابعة |

### مسارات الإرسال المنفصلة: **اثنان**
- **A:** `MessageRouter._on_message → channel.send_message` (`message_router.py:79‑82`) — يبني `OutgoingMessage` بلا `variant_id`، يطبع سطر `[OUT]`.
- **B:** `send_followups.py` → `TelegramChannel().send_message` مباشرة (`:64`, `:86`) — يتجاوز Router كلياً. → F5.

### ما يُسجَّل عن أي رسالة صادرة
`print()` فقط: `[OUT] -> {user}: {reply_text} ({ok|FAILED})` و`[SENT-1/2] -> {user} | {service}`. **إلى stdout، بلا ملف، بلا `variant_id`، يبقى صفر ثانية بعد انتهاء العملية.** لا شيء يربط رسالة صادرة بنتيجة.

### أقل تغيير ليحمل كل صادر `variant_id` ويُنتج `RESPONSE_SENT`
| الملف | التغيير | الحجم |
|---|---|---|
| `channel_interface.py` | حقل `variant_id: Optional[str]` في `OutgoingMessage` | سطر واحد |
| `business_logic.py` | `_decide` يُرجع `(reply, rule_decision, variant_id)` بدل زوج؛ توسيع `handle_message` لتمرير المعرّف | ~10 أسطر، لمس كل فرع `return` |
| `main.py` | `combined_handler` يمرّر `variant_id` (يعيد كائناً/tuple بدل `str`) | ~5 أسطر |
| `message_router.py` | بعد حساب الرد، استدعاء `emit_event("RESPONSE_SENT", lead_id, variant_id, ...)` ثم الإرسال | ~5 أسطر |
| `send_followups.py` | استبدال الاستدعاء المباشر بدالة إرسال مشتركة تُصدر الحدث؛ إسناد `variant_id` لكل قالب | ~10 أسطر |
| **جديد** `events.py` | `emit(type, lead_id, channel, ts, variant_id, payload)` — `open(path,"a")` + `json.dumps` سطر | ~25 سطراً |

يعتمد على وجود `lead_id` مستقر (F7) أولاً.

### «أي صياغة أنتجت طلبات حجز أكثر؟» — **لا، مستحيل اليوم.**
لا `variant_id`، لا حدث، لا `lead_id` للربط بين صادر ونتيجة، ولا بيانات نتيجة نهائية (حضور) أصلاً.

### هل يفصل التصميم بين «رد اقتُرح» و«رد أُرسل فعلاً»؟ — **لا.**
Router يفترض أن `handler` أنتج رداً = سيُرسَل؛ الإشارة الوحيدة `success` (HTTP 200 من `telegram_channel.py:50`) تُطبع وتُهمَل. لا حالة «مقترح». في وضع Concierge (الملحق §2، L6 🔴) هذا الفصل **شرط فوترة**: `FOLLOWUP_SENT` يجب أن يُكتب لحظة ضغط المشغّل «إرسال»، لا لحظة توليد الاقتراح. غير مبني.

---

## القسم 7 — التخزين والأحداث

### ما يُخزَّن فعلاً
- **`data/sessions.json`** (عبر `session_store.py`): `{ user_id: {state, service: {name, keywords[], price}} }`. كتابة ذرية (`.tmp` + `os.replace`) + `threading.Lock`. **يخزّن كائن الخدمة كاملاً** — نسخة ثانية من السعر قد تتقادم.
- **`leads.csv`** (عبر `leads_store.py`): 10 أعمدة بعناوين عربية (`FIELDNAMES` `:41‑52`). `utf-8-sig`. كتابة ذرية. هجرة تلقائية من بنية legacy (`:160‑192`).

### الفصل session / leads
نظيف وظيفياً (جلسة = محادثة جارية، leads = نتائج). لكن: **الخدمة مكرَّرة** في المخزنين، و**السعر مكرَّر ثلاث مرات** (`config` → `session.service.price` → صف `leads`). لا مفتاح مشترك بينهما (F7).

### هل تحمي الأقفال من العملية الموازية؟
`_locked()` (`leads_store.py:100‑120`) = `threading.Lock` ثم `_CrossProcessLock` (ملف `leads.csv.lock` بـ`O_CREAT|O_EXCL`, مهلة 10 ث, poll 50ms). **`main.py` و`send_followups.py` معاً:** كلاهما يمرّان بـ`_locked()` حول القراءة+التعديل+الكتابة الكاملة ⇒ يتسلسلان فعلاً، لا Lost Update بينهما. ✅ ضمن هذا النطاق.
**ثغرات:**
- **`get_leads_eligible_*` تقرأ خارج نفس القفل الذي يُمسكه `mark_*`**: في `send_followups.py` التسلسل = `get_eligible` (قفل يُفتح ويُغلق) → `send_message` (بلا قفل) → `mark_followup_sent` (قفل جديد). عمليتا `send_followups.py` متزامنتان قد تقرآن نفس الصف المؤهل وترسلان **مرتين** قبل أن يعلّم أحدهما. (`main.py` لا يرسل متابعات، فالخطر محصور في تشغيلين للسكربت.)
- **`session_store.py` بلا قفل عابر للعمليات** — `threading.Lock` فقط. اليوم `main.py` الكاتب الوحيد للجلسات ⇒ مقبول، لكنه هشّ لأي مسار مستقبلي.

### ملف القفل عند انتهاء العملية فجأة أثناء حمله
`_CrossProcessLock.__exit__` (`:90‑97`) يحذف الملف — **لا يُنفَّذ عند `SIGKILL` / انقطاع كهرباء**. لا PID، لا فحص `mtime`، لا كشف قفل بائد. النتيجة: `leads.csv.lock` يبقى للأبد ⇒ كل `save_lead` / `mark_*` لاحق ينتظر 10 ث ثم يرمي `TimeoutError` ⇒ **تعطُّل كامل لكتابة الـLeads حتى حذف الملف يدوياً**. 🔴 تشغيلياً.

### أين يعرف Business Logic تفاصيل التخزين؟
جيد نسبياً: `business_logic.py` يستدعي `save_lead(user_id, service_name, channel, status, contact_info)` بوسائط نظيفة — **لا يعرف أسماء الأعمدة العربية ولا مسار `leads.csv` ولا CSV**. يعرف فقط شكل `session` (`session["state"]`, `session["service"]["name"]`) — اقتران بسيط ببنية الجلسة. لا مسارات ملفات في `business_logic.py`. `leads_store.py` يملك كل معرفة CSV، `session_store.py` يملك كل معرفة JSON.

### أقل تغيير لإضافة `events.jsonl` بالإلحاق فقط دون لمس منطق القرار
- **جديد** `events.py`: `emit(type, lead_id, channel, ts, variant_id=None, payload=None)` → `open("events.jsonl","a", encoding="utf-8")` + `json.dumps(..., ensure_ascii=False)` + `"\n"`. ~25 سطراً.
- استدعاءات إلحاقية فقط (لا تغيّر أي فرع قرار) في: `leads_store.save_lead` (`LEAD_CREATED`/`PRICE_QUOTED`/`BOOKING_REQUESTED`/`DECLINED`/`MARKED_UNBOOKED`)، `send_followups` (`FOLLOWUP_SENT`)، `message_router` (`RESPONSE_SENT`). ~8 مواضع سطر واحد.
- الاعتماد الوحيد: `lead_id` مستقر (F7). بدونه الأحداث بلا مفتاح ربط.

(لا اقتراح قاعدة بيانات — كما طُلب.)

---

## القسم 8 — طبقة الـAI

### `USE_AI_INTENT=false` مقابل `true`
- **`false`** (`main.py:44‑48`): `understand_message` **لا تُستدعى إطلاقاً**. صفر اتصال بـOpenAI. `handle_message(ai_intent=None)`. سطر `[COMPARE] ... ai_intent=skipped`. القمع كامل Rule-Based.
- **`true`** (`:50‑62`): استدعاء واحد لـ`gpt-4o-mini` **لكل رسالة، في كل الحالات** (حتى `idle`، حيث النتيجة تُحسب وتُهمَل لأن `_decide` يتجاهل `ai_intent` خارج `awaiting_booking_confirmation`). `recent_history=[]` دائماً. `service_mentioned` يُرجَع ولا يُستخدَم أبداً.

### هل يؤثر AI على سعر / حالة جلسة / سجل Lead؟
- **سعر:** لا. مسار مغلق — `find_service` من `config` هو المصدر الوحيد، و`ai_intent` لا يدخله.
- **حالة جلسة + سجل Lead:** **نعم، بشكل غير مباشر.** داخل `awaiting_booking_confirmation`، `effective_branch = ai_intent` عندما تكون النية ضمن `{confirm_booking, decline, hesitant}` (`business_logic.py:104‑107`):
  - `ai_intent="confirm_booking"` وكانت القاعدة `other`/`hesitant` → `update_session(state="awaiting_contact_info")` ⇒ **AI حرّك حالة الجلسة**.
  - `ai_intent="decline"` → `clear_session` + `save_lead(status="not_ready")` ⇒ **AI كتب Lead**.
  - `PRD §17` صريح: AI **لا يُسمح له** بـ«تغيير حالة جلسة أو حجز» و«تسجيل أو تعديل Lead». تصميم Phase 3B الحالي يخالف هذين البندين. (`rule_decision` يُسجَّل منفصلاً للمقارنة، لكن **السلوك الفعلي** تغيّر بيد الـAI.)

### هل يمكن استبدال المزود دون لمس `business_logic.py`؟
**نعم.** `business_logic.py` لا يستورد `ai_understanding` إطلاقاً؛ التنسيق كله في `main.py`. توقيع `understand_message(text, recent_history, session_state) → {"intent", "service_mentioned"}` عام. الاستبدال يلمس `ai_understanding.py` و`main.py` فقط. → **تأكيد §17 رقم 4 يمر.**

### تكلفة الاستدعاء لكل رسالة وأثرها على تكلفة الـLead
`gpt-4o-mini`, نداء واحد, `temperature=0`, `response_format=json_object`. Prompt نظام ~350–400 توكن + مستخدم قصير + خرج JSON ضئيل ⇒ تقديرياً **~0.0001–0.0002 دولار/رسالة**. عند `false`: صفر. عدد الرسائل لكل Lead **يحتاج معطى ميدانياً** — بدونه لا يمكن تحويله لتكلفة/Lead. الترتيب المتوقع: أقل بكثير من تكلفة رسائل القناة، وغير مادي إلا إن انفتحت حلقة إعادة المطالبة `other` (القسم 9، S7).

### هل الـFallback آمن في كل مسارات الفشل؟
- شبكة / مهلة / JSON غير صالح / `intent` خارج القائمة → `_validate_and_normalize` أو `except Exception` → `FALLBACK_RESULT {intent:"other"}` → لا تجاوز → القواعد تكمل. ✅
- **`SystemExit` (مفتاح `OPENAI_API_KEY` غير مضبوط) يُعاد رفعه** (`ai_understanding.py:117‑118`). `except Exception` في `message_router.py:66` **لا يمسك `SystemExit`** ⇒ يخرج من `_on_message` ⇒ من `_poll_loop` (الذي يمسك `requests` فقط) ⇒ **يُسقط البوت بالكامل عند أول رسالة** إذا `USE_AI_INTENT=true` بلا مفتاح. «فشل صاخب» لكن ليس «Fallback آمن لا يوقف الرد».

### تأكيدات `PRD §17` — واحداً واحداً
1. **`false` ⇒ لا اتصال خارجي + القمع الكامل يعمل** → ✅ (`main.py:44‑48`). ملاحظة: `openai` تبقى تبعية استيراد صلبة (`ai_understanding.py:15` عبر `main.py:21`) حتى في وضع `false`.
2. **لا مسار يجعل مخرَج AI مصدراً لسعر** → ✅.
3. **مخرَج AI خارج القائمة → fallback آمن ولا يوقف الرد** → ✅ للأخطاء العادية؛ ⚠️ `SystemExit` يوقف كل شيء.
4. **تبديل المزود لا يتطلب تعديل `business_logic.py`** → ✅.

### التقييم: **6.5 / 10**
فصل نظيف، مزوّد قابل للاستبدال، وضع افتراضي آمن بلا شبكة، حدود سعر صلبة؛ **لكن** الـAI يحرّك حالة الجلسة ويكتب Lead (خرق صريح لحدّي §17)، ونداء مهدور في حالات لا أثر لها، و`SystemExit` يُسقط البوت وسط الاستطلاع، ولا أثر حدثي لأي قرار AI.

---

## القسم 9 — الأمان التشغيلي (`PRD §18`، 13 بنداً)

| # | القاعدة | الحكم | الدليل |
|---|---|---|---|
| S1 | لا سعر إلا من `config` | ✅ قائمة | `find_service` → `SERVICES` من `config` حصراً. (لكن قد يُرسَل سعر **الخدمة الخطأ** — هذا S2.) |
| S2 | خدمة غامضة → استيضاح | ❌ **مكسورة** | `find_service` أول تطابق، لا كشف تعدد. **Pilot Blocker 🔴** |
| S3 | لا معلومة طبية ولا وعد بنتيجة | ✅ قائمة | كل الردود قوالب ثابتة بلا ادّعاء. |
| S4 | AI يفهم ولا يقرر؛ قائمة مغلقة؛ Fallback آمن | ⚠️ جزئياً | القائمة مغلقة والـfallback موجود، **لكن** AI يقرر فعلياً confirm/decline (§17) و`SystemExit` غير آمن. |
| S5 | كل رد نص معتمد بشرياً | ✅ فعلياً | كل النصوص hardcoded؛ AI لا يكتب نصاً. |
| S6 | `automation_paused` لكل Lead | ❌ **غير موجود** | لا حقل، لا قائمة استثناء، لا مسار. الإيقاف الوحيد = تحرير `leads.csv` يدوياً. **Pilot Blocker 🔴** |
| S7 | حد أقصى صارم لعدد الرسائل لكل Lead | ❌ **مكسور** | فرع `other` في `awaiting_booking_confirmation` (`business_logic.py:121`) يعيد «هل ترغبين بتأكيد...» **بلا حد** لكل رسالة غير معروفة. المتابعات محدودة بـ2، لكن لا سقف كلي. **Pilot Blocker 🔴** |
| S8 | ساعات إرسال آمنة | ❌ **غير موجود** | لا فحص وقت في أي مسار. `send_followups.py` يرسل فور تشغيله؛ `main.py` يرد في أي ساعة. **يمكن الإرسال 3 فجراً.** **Pilot Blocker 🔴** |
| S9 | Idempotency للوارد | ⚠️ قائمة لكن هشّة | `_processed_message_ids` **في الذاكرة فقط** (`message_router.py:50`) — تُفقد عند إعادة التشغيل. `_last_update_id` كذلك في الذاكرة (`telegram_channel.py:33`) ⇒ عند إعادة تشغيل بعد تعطّل قد يُعاد جلب/معالجة رسائل ⇒ Lead مكرر / رد مكرر. |
| S10 | منع تكرار المتابعة عند فشل جزئي | ⚠️ **خلل مؤكَّد** | `send_followups.py:64‑70`: `send_message` ثم `mark_followup_sent` — **غير ذرّيين**. تعطّل/فشل `mark` بعد إرسال ناجح ⇒ إعادة إرسال في التشغيل التالي. وإن أرجع `mark` قيمة `False` (لا تطابق للمفتاح الثلاثي) ⇒ يُطبع `[SENT]` والمرحلة ثابتة ⇒ إعادة إرسال أبدية. **🔴 إن اعتُبر ذلك ثابتاً.** |
| S11 | التعافي من فشل منتصف الدورة | 🟡 مقبول جزئياً | السكربت عديم الحالة بين التشغيلات ويستأنف من `leads.csv`؛ الخطر الوحيد نافذة الإرسال-المزدوج أعلاه. |
| S12 | حماية البيانات الشخصية | ❌ **مكسورة** | `message_router.py:61` يطبع `message.text` كاملاً (يحوي «سارة 0770…») إلى stdout؛ `:82` يطبع الرد؛ `send_followups`/`check_followups` يطبعان `معرف العميل`. `leads.csv` هواتف بنص صريح (محميّ في `.gitignore` فقط ضد Git، لا ضد stdout/إعادة توجيه إلى ملف). لا تنقيح. **Pilot Blocker 🔴** |
| S13 | قابلية التدقيق — كل صادر له أثر | ❌ **غير موجود** | F4. لا سجل. **Pilot Blocker 🔴** |

**إجابات الأسئلة الموجّهة:**
- **رسالة 3 فجراً؟** نعم. لا شيء يمنعها — لا `SAFE_HOURS`, لا فحص `datetime.now().hour` في `business_logic` ولا `send_followups`.
- **الحد الأقصى الفعلي للرسائل لـLead واحد؟** غير محدود عملياً: 1 عرض سعر + **حلقة إعادة مطالبة `other` بلا سقف** + 2 متابعة. لا عدّاد كلي.
- **إيقاف الأتمتة لعميلة واحدة؟** لا وسيلة مدمجة. فقط تحرير/حذف صفها في `leads.csv` أو ضبط `نتيجة المتابعة` يدوياً.
- **فشل متابعة جزئي في منتصف الدورة؟** الصفوف قبل نقطة الفشل مُعلَّمة، ما بعدها لا؛ التشغيل التالي يستأنف. الصف عند نقطة الفشل: إن أُرسل ولم يُعلَّم ⇒ يُعاد إرساله.
- **نفس المتابعة مرتين؟** نعم — نافذة `send → mark` (`send_followups.py:64‑70`)؛ + عمليتا سكربت متزامنتان تقرآن نفس المؤهل خارج قفل مشترك؛ + `mark` يُرجع `False` ⇒ إعادة أبدية.
- **البيانات الشخصية:** الهواتف والأسماء في `leads.csv` (نص صريح) + نص الرسائل كاملاً في stdout. لا تشفير، لا تنقيح، تتسرب لأي `*.log` أو إعادة توجيه طرفية.

---

## القسم 10 — الاختبارات

**الواقع: NO TESTS.** صفر ملفات `test_*` / `*_test` / `conftest.py` / `pytest.ini` / CI.

### أقل مجموعة اختبارات تجعل تعديل `business_logic.py` آمناً — مرتّبة حسب المخاطرة المُغطّاة

| # | الاختبار | المخاطرة المُغطّاة |
|---|---|---|
| 1 | **دورة حياة الـLead الصامت:** عرض سعر → لا رد → يوجد سجل بـ`lead_id` مستقر، يدخل المتابعة، ينتهي بعد النوافذ | F1 — شرط خروج Gate A الحرفي |
| 2 | **فخ `awaiting_contact_info`:** رسالة عشوائية أثناءها **لا** تُخزَّن كبيانات تواصل ولا تُنشئ حجزاً؛ الجلسة تُعاد تعيينها بعد مهلة | F9 — تلفيق بيانات في `leads.csv` |
| 3 | **الغموض:** `config` بثلاث خدمات ليزر + «شكد سعر الليزر؟» → **سؤال استيضاح، لا سعر**؛ لهجات («فلر»، «بوتكس») لا تُنتج تطابقاً خاطئاً | F6 / S2 — Pilot Blocker |
| 4 | **الدلالات:** Lead عند تسليم الهاتف **لا** يُسمّى `confirmed`؛ `compute_recovery_metrics` لا تعدّ رقماً كـ«إيراد» قبل الحضور | F2 / F3 — الرقم المفوتَر |
| 5 | **مسار إرسال موحّد + حدث:** رد حي ومتابعة كلاهما يمرّان بمسار واحد ويُنتجان `RESPONSE_SENT` واحداً بـ`variant_id` | F4 / F5 |
| 6 | **حدود §17:** `USE_AI_INTENT=false` → صفر اتصال شبكي؛ مخرَج AI لا يصير مصدر سعر أبداً؛ نية خارج القائمة → fallback والرد يُرسَل؛ AI لا يكتب Lead ولا يغيّر حالة | تأكيدات §17 الأربع + خرق حدّ الحالة/الـLead |
| 7 | **Idempotency + المتابعة المزدوجة:** إعادة تشغيل لا تُعيد معالجة رسالة؛ `send` ثم تعطّل قبل `mark` لا يُنتج متابعة ثانية | S9 / S10 |
| 8 | **التزامن:** كاتبان لـ`leads.csv` لا يفقدان تحديثاً؛ ملف قفل بائد بعد `SIGKILL` يُكتشَف ويُتعافى منه | قسم 7 — تعطُّل كتابة الـLeads |

---

## القسم 11 — الحكم والخطة

### أ) الاختيار: **B — Refactor محدود ثم استمرار**

- **ليس A:** F1/F2/F3/F4/F7 ليست «تعديلات صغيرة» — هي غياب عمود فقري للأحداث + مفردات حالة مضلِّلة + رقم مفوتَر خاطئ بنيوياً. لا يُغلق أي منها بسطر.
- **ليس C/D:** فصل الطبقات **حقيقي وليس ادّعاءً**: `channel_interface` عقد نظيف، `session_store` مالك وحيد فعلي، `config` خارجي بتحقق صارم، `business_logic` منفّذ لا stub، `leads_store` يملك كل تفاصيل CSV. كلفة إعادة البناء **أعلى بوضوح** من كلفة الإصلاح، ولا دليل من الكود على العكس (شرط `PRD §11` لقبول C/D غير مستوفى).
- **B** لأن الإصلاحات السبعة أدناه تلمس 6 ملفات موجودة + ملفين جديدين صغيرين، بلا مساس بعقد القناة ولا بشجرة القرار الجوهرية.

### ب) 5–7 تغييرات لتحقيق شروط خروج Gate A (`PRD §20`)، مرتّبة بحيث لا يكسر تغيير ما قبله

| # | التغيير | الهدف | الملفات | جديد/موجود | الحجم | الاعتماديات | لماذا لا يمكن تأجيله |
|---|---|---|---|---|---|---|---|
| 1 | **`lead_id` مستقر + مفتاح هوية `(channel, external_user_id)`** | F7, D3, D4 | `leads_store.py` (+عمود+هجرة) | موجود | M | — | كل حدث وكل ربط صادر↔حجز يحتاجه؛ بناؤه بعد الأحداث = إعادة هجرة الأحداث |
| 2 | **إنشاء Lead لحظة عرض السعر + تمثيل الصمت** | F1, D1 | `business_logic.py`, `leads_store.py` | موجود | M | 1 | «كل Lead مرئي» شرط خروج Gate A الحرفي |
| 3 | **مواءمة مفردات الحالة مع §8** (`price_quoted`/`booking_requested`/`declined`/`unbooked`؛ حذف `confirmed`/`not_ready`؛ إزالة احتساب الإيراد عند الطلب) | F2, F3, D2 | `leads_store.py`, `business_logic.py`, `send_followups.py`, `lead_recovery_report.py` | موجود | M | 2 | «لا مصطلح في الكود يخالف §8» شرط خروج؛ كل مقياس لاحق خاطئ حتى يُصلَح |
| 4 | **`events.jsonl` بالإلحاق فقط + إصدار حدث عند كل انتقال** | F4, D3 | **جديد** `events.py`؛ `leads_store.py`, `business_logic.py`, `message_router.py`, `send_followups.py` | مختلط | M‑L | 1, 3 | دليل Gate A = «تقرير قمع مشتق من `events.jsonl` وحده» |
| 5 | **توحيد مسار الإرسال + `variant_id`** | F5, D3 | `channel_interface.py`, `message_router.py`, `send_followups.py`, `business_logic.py`, `main.py` | موجود | M | 4 | مساران = قواعد مكرّرة؛ «كل رسالة عبر مسار واحد تنتج حدثاً بـ`variant_id`» شرط خروج |
| 6 | **`find_service`: كشف التعدد → سؤال استيضاح** (حالة جلسة `awaiting_service_disambiguation`) | F6, S2, D5 | `services.py`, `business_logic.py` | موجود | M | 3 (حالة جديدة), 2 | S2 Pilot Blocker؛ «الخدمة الغامضة تنتج سؤالاً لا سعراً» شرط خروج |
| 7 | **دورة حياة الجلسة: TTL/إعادة تعيين + إغلاق فخ `awaiting_contact_info`** | F9 | `session_store.py`, `business_logic.py` | موجود | S‑M | 2, 3 | اليوم رسالة العميلة الصامتة التالية تُساء تفسيرها وقد تُنشئ حجزاً وهمياً — يفسد الملف الذي يجب أن يوثَّق في Gate A |

**F8:** اختبارات القسم 10 (1–8) + تأكيدات §17 تُكتب خلال 1–7، وهي شرط خروج Gate A («اختبارات خضراء»)، لا تغيير مرتَّب منفصل.

### ج) الخلاصة التنفيذية

**WHAT ACTUALLY WORKS**
تجريد القناة (`channel_interface`) وعزل الأخطاء + Fallback في Router؛ Idempotency داخل العملية عبر `(channel, message_id)`؛ التسعير من `config` مع تحقق صارم عند الإقلاع؛ المسار السعيد للـVertical Slice (سعر → «نعم» → هاتف → صف)؛ آلة حالة المتابعة على مرحلتين + Expiry؛ صمود الجلسة بعد إعادة التشغيل؛ قفل عابر للعمليات على `leads.csv`؛ الـAI مطفأ افتراضياً وعاجز عن تحديد سعر.

**WHAT SILENTLY FAILS**
الـLead الصامت لا يُسجَّل أبداً؛ أي رسالة أثناء `awaiting_contact_info` تصير «بيانات تواصل» + حجز `confirmed`؛ جلسة عالقة تحوّل كل رسالة لاحقة لرد «نعم/لا» على سؤال ميت؛ متابعة أُرسلت ولم تُعلَّم ⇒ إعادة إرسال أبدية؛ ملف قفل بائد بعد `SIGKILL` يجمّد كل كتابة Leads؛ `send_followups.py` لا يعمل تلقائياً ⇒ Leads مجمّدة في المرحلة 0؛ «لا» كسلسلة فرعية تبتلع «يلا نحجز».

**WHAT MEANS SOMETHING DIFFERENT THAN ITS NAME**
`status="confirmed"` = أرسلت هاتفاً، لا تأكيد موظفة؛ `revenue_recovered` = مجموع أسعار مقتبَسة لحظة الطلب، لا إيراد؛ «سعر الخدمة وقت الإنشاء» = السعر لحظة كتابة الصف لا لحظة العرض؛ `not_ready` = «لا» صريحة فقط، لا الأغلبية الصامتة؛ `awaiting_booking_confirmation` = بانتظار العميلة، لا حجز مؤكَّد؛ `bookings_recovered` == `leads_recovered` (نفس القائمة تُعدّ مرتين).

**BIGGEST COMMERCIAL RISK**
F3 — الرقم الوحيد المفوتَر عليه (`revenue_recovered`) يحتسب المال لحظة طلب الحجز، بلا أي حضور في النظام ولا مسار للحصول عليه (الملحق §4: الوصولات خارج وصولنا). كل فاتورة ستضخّم.

**BIGGEST SAFETY RISK**
F6 / S2 — سعر الخدمة الخطأ يُرسَل بصفر كشف للغموض، بالتساوي مع S8 (رسالة 3 فجراً) وS6 (لا وسيلة لإيقاف الأتمتة لعميلة واحدة). ثلاثتها Pilot Blockers مفتوحة.

**FIRST 3 ACTIONS**
1. `lead_id` مستقر + مفتاح هوية `(channel, external_user_id)`.
2. إنشاء Lead لحظة عرض السعر + تمثيل الصمت كحالة.
3. مواءمة مفردات الحالة مع §8 وحذف احتساب الإيراد عند الطلب — ثم العمود الفقري `events.jsonl`.

---

*لم يُنتقَل إلى التنفيذ. بانتظار مراجعتكم للتقرير.*
