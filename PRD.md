# PRD — Beauty Clinic Revenue Growth Engine

> **الحالة:** نهائية — المرجع المعتمد. تُلغي PRD v2 وv3.
> **الموقع المقصود:** جذر المستودع باسم `PRD.md`
> **الأساس:** الكود الفعلي في `main` (قراءة 27 أغسطس 2026)
> **قاعدة التنفيذ:** لا تنفيذ ولا Refactor استناداً لهذه الوثيقة قبل Architecture Audit ومراجعته واعتماده.

---

## 0. كيف تُقرأ هذه الوثيقة

هذه وثيقة قرار، لا وثيقة أمنيات. كل رقم فيها إما **محسوم** أو موسوم صراحةً بـ`TO VALIDATE`.
لا يوجد رقم مُخترع. غياب رقم يُكتب «غير معروف»، لا يُقدَّر.

| الوسم | المعنى |
|---|---|
| 🔴 | يمنع الـPilot أو يمنع عبور Gate — غير قابل للتأجيل |
| 🟡 | مهم، يمكن تأجيله بقرار موثّق |
| 🟢 | لاحق |
| `TO VALIDATE` | افتراض قابل للاختبار، ممنوع بناء قرار عليه قبل التحقق |

**تسلسل المرجعية:** هذه الوثيقة تحكم في المعاني والأولويات والحدود. الكود يحكم في وصف الواقع. عند التعارض: يُصحَّح أحدهما صراحةً، ولا يُتجاهل.

---

## 1. Executive Summary

**نحن نبني أصغر نظام قادر على إثبات ما إذا كان يزيد الحجوزات المكتملة وإيرادات عيادة تجميل.**

لا نبني أذكى چات بوت للعيادات.

المحادثة قناة تنفيذ داخل النظام. النجاح يُقاس بالإيراد الإضافي القابل للإثبات، لا بجودة المحادثة.

| | |
|---|---|
| **المنتج** | Beauty Clinic Revenue Growth Engine |
| **ما يفعله اليوم** | يستقبل استفسارات العميلات، يرد بسعر من مصدر حقيقة واحد، يطلب الحجز، ويتابع من لم يحجز |
| **ما يبيعه** | حجوزات مكتملة أكثر + فرص أقل ضياعاً + **رقم واحد قابل للدفاع عنه** يثبت أن النظام هو السبب |
| **ما لا يبيعه** | ذكاء، محادثة طبيعية، «AI للعيادات»، لوحات تحكم |
| **الحالة الحقيقية** | الأساس مبني ويعمل. المشكلة في الدلالات والقياس، لا في المعمارية |
| **الخطوة التالية** | Gate A (سلامة الأساس) بالتوازي مع Commercial Validation ميدانياً |

**اختبار كل قرار في هذا المشروع، بلا استثناء:**

> هل يحرّك هذا واحداً من: Conversion، Lead Recovery، No-show، Retention، Revenue، **القدرة على قياس الأثر**، أو تكلفة التشغيل؟
> إن لا → مؤجَّل، مهما كان جميلاً هندسياً.

---

## 2. تعريف المنتج وحدوده

### ما هو النظام

محرك يعمل على قمع تجاري محدد: يلتقط كل استفسار، يمنعه من الضياع، ويحوّله إلى حجز مكتمل — ثم **يقيس أثره على ذلك بمجموعة ضابطة**.

### ما ليس النظام (حدود صريحة)

| ليس | لماذا |
|---|---|
| Chatbot | المحادثة وسيلة. لو أثبتت البيانات أن نموذجاً بلا محادثة يزيد الإيراد أكثر، نبنيه |
| AI Assistant / AI Agent | AI طبقة فهم اختيارية، لا محور. النظام يجب أن يعمل كاملاً بدونها |
| CRM | لا نبني سجل عملاء ولا إدارة علاقات. نبني قياس قمع |
| Dashboard كمنتج | التقارير مخرَج، لا منتج. تقرير نصي دوري يكفي حتى Gate E |
| نظام حجوزات | لا تقويم ولا إدارة مواعيد. النظام يسلّم طلب حجز ويستقبل نتيجته |

### الوظائف داخل النطاق الحالي

التقاط الـLead · الرد بسعر من مصدر الحقيقة · كشف الغموض والاستيضاح · طلب الحجز · تسليمه للعيادة · متابعة من لم يحجز · تسجيل كل حدث · استقبال نتيجة الحجز من العيادة · إنتاج تقرير قمع وإسناد وأثر إضافي.

كل ما عدا ذلك خارج النطاق حتى Gate E.

---

## 3. الحالة الحقيقية للنظام

### ما يعمل

| المكوّن | الدليل |
|---|---|
| Channel Layer | `channel_interface.py` عقد مجرّد نظيف؛ `telegram_channel.py` التطبيق الوحيد |
| Message Router | Idempotency عبر `(channel, message_id)`؛ عزل الأخطاء؛ Fallback |
| Vertical Slice | `business_logic._decide()`: استفسار → سعر حقيقي من `config/clinic_config.json` → عرض حجز → جمع بيانات → `save_lead()` |
| Follow-up | `send_followups.py`: متابعة 24س و72س + Expiry + تمييز «مسترجَع»/«أُغلق» |
| Session Persistence | `storage/session_store.py` مالك وحيد، كتابة ذرية، يصمد بعد إعادة التشغيل |
| Config خارجي | خدمات وأسعار خارج الكود مع تحقق صارم عند الإقلاع |
| قفل التزامن | قفل مزدوج (خيوط + ملفي) + كتابة ذرية في `leads_store.py` |
| حدود AI | `USE_AI_INTENT=false` افتراضياً؛ `AI_OVERRIDE_ALLOWED` ثلاث نوايا فقط |

**الحكم: فصل الطبقات حقيقي وليس ادّعاءً في README.** الخلل في المعاني والقياس.

### الفجوات المؤكدة

| # | الفجوة | الدليل | الأثر |
|---|---|---|---|
| **F1** 🔴 | الـLead الصامت غير مسجَّل | `save_lead()` تُستدعى فقط عند تسليم البيانات أو رفض صريح | الأغلبية الساحقة من الـLeads لا يراها النظام. نبيع استعادة الضائع بينما لا نرى الضائع |
| **F2** 🔴 | تصادم دلالي في `confirmed` | تُكتب لحظة استلام الهاتف، بينما تعني تجارياً تأكيد الموظفة | كل معادلة إيراد خاطئة بمقدار فجوة التأكيد + الحضور |
| **F3** 🔴 | الإيراد يُحتسب عند الطلب لا الحضور | `compute_recovery_metrics()` تجمع الأسعار لصفوف «مسترجَع» | تضخيم مباشر لرقم يُفوتَر عليه — أخطر خطأ تجاري ممكن |
| **F4** 🔴 | لا سجل أحداث إطلاقاً | لا أثر لأي رسالة صادرة في أي مكان | تتبع أثر الردود مستحيل تقنياً |
| **F5** 🔴 | مساران للإرسال | `send_followups.py` ينشئ قناته ويتجاوز `MessageRouter` | كل قاعدة على الرسائل الصادرة تُكرَّر مرتين ومصدر تباعد مؤكد |
| **F6** 🔴 | مطابقة الخدمة تخمينية | `find_service()` تُرجع أول تطابق؛ لا مفهوم للغموض | سعر خاطئ لعميلة حقيقية — خرق Safety Rail |
| **F7** 🔴 | لا `lead_id` مستقر | المفتاح ثلاثي (معرّف+خدمة+وقت)؛ الهوية = معرّف القناة | ربط الرد بالحجز هشّ؛ بيانات الأداء تُفقد عند تغيير القناة |
| **F8** 🟡 | صفر اختبارات | لا ملفات test | كل تعديل على منطق القرار مخاطرة عمياء |

**ملاحظات:** `stub_business_logic` قيمة افتراضية غير مستخدمة (مصدر الالتباس الأصلي في v2) · README يصف بنية `channel_layer/core/` بينما الملفات في الجذر · `.gitignore` يحمي `leads.csv` و`data/` بشكل صحيح.

---

## 4. القرارات المحسومة

غير قابلة لإعادة النقاش في الـAudit. دور الـAudit تحديد **أقل تغيير** ينفذها.

| # | القرار |
|---|---|
| **D1** | يُنشأ سجل Lead عند لحظة الرد بالسعر. الصمت حالة مشروعة قابلة للمتابعة |
| **D2** | تسميات الحالات تطابق التعريف التجاري: `booking_request` / `confirmed_booking` / `completed_booking` |
| **D3** | البنية الدنيا للتتبع أربعة عناصر: `lead_id` مستقر، `variant_id` في `OutgoingMessage`، مسار إرسال موحّد، `events.jsonl` بالإلحاق فقط |
| **D4** | الهوية مفتاح مركّب `(channel, external_user_id)`. لا Identity Resolution. عميل Telegram ≠ عميل WhatsApp إلا بدليل |
| **D5** | الغموض حالة أولى الدرجة: خدمة غير محددة → سؤال استيضاح، لا تخمين |
| **D6** | Clinic Feedback Loop جزء من المعمارية الأساسية، لا تكامل خارجي (§11) |
| **D7** | نسبة الـHoldout معطى قابل للضبط، يُحدَّد من الـBaseline قبل الـPilot (§10) |
| **D8** | الفوترة على وحدات منسوبة، والقياس الإحصائي يعاير **السعر** لا **العدد** (§9.4) |

---

## 5. مسار الإيراد ومصفوفة الملكية

```
[خارج]    إعلان / انستغرام / إحالة / لافتة
   ↓
[دخول]    رسالة أولى                    → LEAD_CREATED
   ↓
[النظام]  فهم النية + تحديد الخدمة        → PRICE_INQUIRY | AMBIGUOUS_SERVICE
   ↓
[النظام]  رد بسعر + عرض حجز             → RESPONSE_SENT (variant_id)
   ↓
   ├── وافقت وسلّمت بياناتها             → BOOKING_REQUESTED
   ├── رفضت صراحة                       → DECLINED
   └── صمتت                             → UNBOOKED (بعد نافذة الصمت)
   ↓
[النظام]  متابعة (إن لم تكن في الضابطة)   → FOLLOWUP_SENT (variant_id)
   ↓
   └── حجزت بعد المتابعة                 → BOOKING_REQUESTED (attributed: followup_assisted)
   ↓
══════════ حدّ النظام ══════════
   ↓
[العيادة] الموظفة تؤكد الموعد            → BOOKING_CONFIRMED    ← إدخال بشري
   ↓
[العيادة] حضرت أو لم تحضر               → COMPLETED | NO_SHOW   ← إدخال بشري
   ↓
[الإيراد] Revenue = Σ أسعار الـCompleted فقط
   ↓
[الإثبات] مقارنة بالمجموعة الضابطة        → Incremental Revenue
   ↓
[التطوير] ترتيب الـVariants حسب النتيجة النهائية
```

### مصفوفة الملكية والإثبات

| المرحلة | من يملكها | من ينتج البيانات | قابلة للإثبات؟ | قابلة للفوترة؟ |
|---|---|---|---|---|
| وصول الـLead | العيادة (تسويقها) | النظام | نعم | لا |
| سرعة الرد | **النظام** | النظام | نعم | لا (جزء من الاشتراك) |
| صحة السعر | **النظام** | النظام | نعم | لا |
| Booking Request | **النظام** | النظام | نعم | لا — طلب ليس إيراداً |
| Confirmed Booking | **العيادة** | العيادة | نعم بشرط التغذية | لا |
| Completed Booking | **العيادة** | العيادة | نعم بشرط التغذية | **نعم** |
| قيمة الخدمة | العيادة | `clinic_config` | نعم | نعم |
| الأثر الإضافي | مشترك | مقارنة المجموعتين | نعم على مستوى المجموعة | لا مباشرة — يعاير السعر (§9.4) |

**الخلاصة الحاسمة:** النظام يملك نصف القمع الأعلى بالكامل، ولا يملك أي شيء بعد `BOOKING_REQUESTED`. **الإيراد يتحقق خارج النظام.** لذلك حلقة الرجوع من العيادة ليست تكاملاً اختيارياً بل شرط وجود للمنتج التجاري (§11).

### نقاط تسرّب القياس

| التسرّب | الوصف | العلاج |
|---|---|---|
| L1 | عميلة تحجز عبر مكالمة أو حضور مباشر بعد قراءة الرد | لا يُنسب — يُقبل كنقص محافظ في تقديرنا، ويُذكر للعيادة |
| L2 | الموظفة ترد يدوياً بالتوازي مع النظام | يُفسد الإسناد — الحل: المجموعة الضابطة تلتقط هذا الأثر تلقائياً |
| L3 | العيادة لا تغذّي الحضور | ينهار القياس والفوترة معاً — القاعدة: لا إثبات = لا رسوم |
| L4 | تغيّر السعر بين الاستفسار والحضور | يُلتقط سعر وقت الاستفسار كصورة (موجود في الكود) + سعر وقت الحضور |
| L5 | موسمية أو حملة إعلانية أثناء الـPilot | **أقوى حجة للمجموعة الضابطة المتزامنة** بدل مقارنة قبل/بعد |

---

## 6. Domain Model (الحد الأدنى)

```
Clinic ──< Service
   │
   └──< Customer   (channel + external_user_id)   ← لا دمج بين القنوات
            │
            └──< Lead   (lead_id مستقر، خدمة واحدة، holdout_flag، consent، automation_paused)
                   │
                   ├──< Message  (وارد أو صادر؛ الصادر يحمل variant_id)
                   └──0..1 Booking  (states: requested → confirmed → completed | no_show | lapsed)

Event  ← سجل بالإلحاق فقط، مصدر الحقيقة لكل تحليل لاحق
```

**`Lead` = نية تجارية واحدة تجاه خدمة واحدة.** عميلة تسأل عن خدمتين = Leadان. هذا يبقي الإسناد نظيفاً بلا تعقيد.

**ممنوع في هذه المرحلة:** Identity Resolution · Multi-tenancy · PostgreSQL · جداول علاقات عملاء · تفضيلات · تاريخ طبي. تُستأنف فقط إذا أثبت الـAudit أنها تمنع Gate A أو B.

### أنواع الأحداث (الحد الأدنى)

`LEAD_CREATED` · `PRICE_QUOTED` · `AMBIGUITY_ASKED` · `RESPONSE_SENT` · `BOOKING_REQUESTED` · `DECLINED` · `MARKED_UNBOOKED` · `HOLDOUT_ASSIGNED` · `FOLLOWUP_SENT` · `BOOKING_CONFIRMED` · `BOOKING_COMPLETED` · `NO_SHOW` · `AUTOMATION_PAUSED`

كل حدث يحمل: `event_id`, `lead_id`, `channel`, `timestamp`, `variant_id?`, `payload`.
**قاعدة:** كل تقرير يُشتق من `events.jsonl`. `leads.csv` يصبح عرضاً مشتقاً، لا مصدر حقيقة.

---

## 7. دورات الحياة

### الـLead

```
NEW ──استفسار──> PRICE_QUOTED ──┬── وافقت ──> BOOKING_REQUESTED
                                 ├── رفضت ───> DECLINED
                                 └── صمتت ───> UNBOOKED
                                                 ├─ holdout=true ──> (لا متابعة أبداً)
                                                 └─ holdout=false ─> FOLLOWUP_1 ─> FOLLOWUP_2 ─> EXPIRED
                                                                        └──────────┴──> BOOKING_REQUESTED
AMBIGUOUS_SERVICE → استيضاح → PRICE_QUOTED
```

### الحجز

```
REQUESTED ──تأكيد الموظفة──> CONFIRMED ──┬── حضرت ──> COMPLETED  → إيراد
     │                                    └── لم تحضر ──> NO_SHOW
     └── لم تُؤكَّد خلال المهلة ──> LAPSED
```

كل انتقال بعد `REQUESTED` مصدره بشري حصراً. لا يُسمح لأي منطق آلي بكتابته.

---

## 8. المصطلحات والمقاييس

> أي مصطلح غير موجود هنا ممنوع استخدامه في الكود أو التقارير أو أمام عيادة قبل إضافته.

| المصطلح | التعريف |
|---|---|
| Lead | نية تجارية واحدة بدأت بمحادثة، تجاه خدمة واحدة |
| Qualified Lead | Lead وصل `PRICE_QUOTED` |
| Unbooked Lead | Qualified Lead لم يصل `BOOKING_REQUESTED` خلال نافذة الصمت |
| Booking Request | العميلة طلبت الحجز وسلّمت بياناتها — قبل تأكيد الموظفة |
| Confirmed Booking | الموظفة أكّدت الموعد |
| Completed Booking | العميلة حضرت فعلاً |
| No-show | Confirmed لم يحضر |
| Recovered Lead | Unbooked تلقّى متابعة ثم أنتج Booking Request |
| Recovered Completed Booking | Recovered Lead وصل إلى Completed — **وحدة الفوترة الوحيدة** |
| Holdout / Control | Unbooked مستثنى من كل متابعة آلية |

### طبقات الإيراد

```
Potential Revenue  = Σ سعر الخدمة لكل Qualified Lead      ← حجم الفرصة، ليس وعداً
Requested Revenue  = Σ سعر الخدمة لكل Booking Request     ← مؤشر مبكر
Booked Revenue     = Σ سعر الخدمة لكل Confirmed Booking   ← مؤشر متوسط
Revenue            = Σ سعر الخدمة لكل Completed Booking   ← الوحيد الحقيقي
```

🔴 **قاعدة ثابتة: لا يُسمّى رقم «إيراداً» إلا عند الحضور.** الطبقات الثلاث العليا تُسمّى بأسمائها الكاملة دائماً — في الكود، في التقارير، وفي أي عرض بيع. `compute_recovery_metrics()` الحالية تخالف هذا (F3).

### المعدلات

| يقيس النظام | يقيس العيادة |
|---|---|
| Response Rate = مردود عليها ÷ كل Leads | Confirmation Rate = Confirmed ÷ Requests |
| Quote Rate = Qualified ÷ Leads | Show Rate = Completed ÷ Confirmed |
| Request Rate = Requests ÷ Qualified | No-show Rate = No-shows ÷ Confirmed |
| Recovery Rate = Recovered ÷ Unbooked | |

الفصل مقصود: هو ما يمنع نزاع الإسناد لاحقاً. لا نتحمل مسؤولية معدل يقيس أداء العيادة، ولا ندّعي فضله.

**Conversion Rate = Completed Bookings ÷ Qualified Leads** — المقياس المشترك، ويُقرأ دائماً مع المعدلين المسؤولة عنهما العيادة.

---

## 9. القياس: ثلاث طبقات منفصلة تماماً

هذا أهم قسم تجارياً. الخلط بين الطبقات الثلاث هو ما يفجّر العلاقة مع العيادة.

### 9.1 Attribution — وصف، لا سببية

| الفئة | القاعدة |
|---|---|
| `organic` | Booking Request بلا أي `FOLLOWUP_SENT` سابق لنفس الـLead |
| `followup_assisted` | Booking Request بعد متابعة واحدة أو أكثر |
| `unknown` | لا يمكن ربطه بـLead واضح |

**ما يعنيه:** «حجزت بعد رسالتنا». **ما لا يعنيه:** «ما كانت لتحجز لولاها».
موجود تقريباً في الكود عبر «مسترجَع»/«أُغلق»، ويحتاج محاذاة مصطلحات فقط.

### 9.2 Incrementality — سببية، على مستوى المجموعة حصراً

```
Incremental Recovery Rate = Recovery Rate(معالَجة) − Recovery Rate(ضابطة)
Incrementality Ratio (IR) = Incremental Recovery Rate ÷ Recovery Rate(معالَجة)
Incremental Revenue = عدد Unbooked × Incremental Recovery Rate × متوسط قيمة الحجز المكتمل
```

🔴 **قاعدة:** Incrementality **لا تُنسب أبداً لحجز فردي**. لا يوجد شيء اسمه «هذا الحجز إضافي». الإضافية خاصية إحصائية لمجموعة، لا لشخص. أي جملة في تقرير أو عرض بيع تخالف هذا تُحذف.

### 9.3 Billing — تعاقدية، على مستوى الحجز الفردي

وحدة الفوترة الوحيدة: **Verified Recovered Completed Booking**، وهي حجز استوفى الخمسة معاً:

1. الـLead كان `UNBOOKED`
2. الـLead كان في المجموعة المعالَجة (لا الضابطة)
3. تلقّى متابعة واحدة على الأقل موثّقة بحدث `FOLLOWUP_SENT`
4. `BOOKING_REQUESTED` وقع **بعد** المتابعة زمنياً
5. العيادة أكّدت الحضور (`BOOKING_COMPLETED`)

هذه وحدة **منسوبة**، وقابلة للتحقق من الطرفين، ولا تحتاج جدلاً إحصائياً.

### 9.4 الجسر بين القياس والفوترة 🔴

المشكلة: الفوترة على وحدات منسوبة ستشمل حجوزات كانت ستحدث بلا تدخلنا.
الحل الذي نعتمده — **القياس يعاير السعر، لا العدد**:

```
الفاتورة = عدد الوحدات المنسوبة المتحققة × الرسم الثابت لكل وحدة
الرسم الثابت يُسعَّر ابتداءً على أساس IR المتوقع، ويُعاد معايرته دورياً بـIR المقيس فعلياً
```

مثال توضيحي للآلية (أرقام افتراضية بحتة): إذا قِيس IR = 0.6، فإن 40% من الوحدات المنسوبة كانت ستحدث بلا تدخلنا — فيُخفَّض الرسم لكل وحدة بما يعكس ذلك في الدورة التالية.

**لماذا هذا التصميم:**
- العدد يبقى بسيطاً وقابلاً لتحقق الطرفين — **لا نزاع على أي حجز فردي**
- الدقة الإحصائية تدخل حيث تنتمي: في السعر، لا في الجدل حول أسماء
- إذا انهارت جودة القياس، ينهار السعر تدريجياً — لا العلاقة

**بند العقد الحرفي المقترح:**
> الرسم لكل وحدة مسعَّر مسبقاً على أساس أن نسبة من الحجوزات المنسوبة كانت ستحدث بلا تدخل النظام. لا يدّعي المزود أن أي حجز فردي سببه النظام. المعايرة الدورية للرسم هي آلية التصحيح الوحيدة.

🔴 **القاعدة الحاكمة: إذا لم نستطع إثبات الحضور، لا نستحق رسم أداء.** بلا استثناء، وبلا تقدير، وبلا «ترجيح».

---

## 10. تصميم الـHoldout

### المبادئ (ثابتة)

- إسناد **حتمي** من `hash(lead_id)` — لا عشوائية زمنية، قابل لإعادة الحساب والتدقيق
- يُسنَد **مرة واحدة عند `UNBOOKED`** ولا يتغير أبداً
- المجموعة الضابطة **لا تتلقى أي متابعة آلية**، وتُعامل كما كانت العيادة تعاملها قبلنا
- **لا يُحجب أي شيء طلبته العميلة.** أي رسالة منها تتلقى رداً كاملاً فورياً. الحجب يقتصر على المتابعة التسويقية غير المطلوبة
- **لا تُستخدم أي صفة شخصية** في التقسيم — تجزئة من المعرّف فقط
- تُعرض المجموعة الضابطة صراحةً على العيادة في العقد. لا تُخفى

### النسبة: معطى قابل للضبط 🔴

`holdout_percentage` معطى إعداد، **يُحدَّد قبل الـPilot** بعد توفر:
عدد الـUnbooked شهرياً · Baseline Recovery Rate · متوسط قيمة الحجز المكتمل · مدة الـPilot · أصغر أثر يستحق الاكتشاف (MDE).

**قاعدة الاختيار عند Gate C:**

| الحالة | التوصية |
|---|---|
| حجم الـLeads كبير نسبياً | 10–20% — يقلل تكلفة الفرصة ويكفي إحصائياً |
| حجم الـLeads صغير (الأرجح لعيادة واحدة) | **قريب من 50%** — التوزيع المتساوي يعظّم القدرة الإحصائية لعينة ثابتة |
| صغير جداً بحيث لا يكفي بأي نسبة | لا يُشغَّل الـHoldout — يُعلَن صراحةً أن Gate D سيُثبت الإسناد لا الإضافية، ويُؤجَّل الإثبات لعيادتين أو فترة أطول |

⚠️ **التحذير الحاسم:** 20% ليست رقماً محايداً. اختيارها يقلل تكلفة الفرصة لكنه **يضعف القدرة الإحصائية** — والقدرة هي المنتج نفسه هنا. عيادة واحدة صغيرة قد لا تنتج دلالة بأي نسبة، وهذا يجب أن يُعرف **قبل** الـPilot لا بعده.

**ما لا نبنيه:** منصة تجارب، طبقات، أوزان، Multi-touch، Bandits. المطلوب: حقل واحد + شرط استثناء واحد في محرك المتابعة.

---

## 11. Clinic Feedback Loop 🔴 (مكوّن أساسي)

**القرار: حلقة الرجوع جزء من المعمارية الأساسية، لا تكامل خارجي.**

بدونها لا يوجد Completed Booking، ولا Revenue، ولا Incremental Revenue، ولا نموذج تجاري. أي بناء يؤجلها يبني منتجاً لا يمكن بيعه.

### المستخدم الحقيقي

**الموظفة، لا صاحب العيادة.** هي من ستُدخل البيانات، وهي ليست من يدفع، وليس لديها حافز. أي تصميم يتجاهل هذا يفشل في الأسبوع الثاني — بغض النظر عن جودة الكود.

### التصميم الأدنى: نقطتا لمس، ثانيتان لكل واحدة

| اللحظة | ما يصل الموظفة | ما تفعله |
|---|---|---|
| عند `BOOKING_REQUESTED` | إشعار في محادثة الطاقم بنفس البوت: الاسم، الخدمة، الهاتف، الوقت | زر واحد: **أكّدت** / **لم يتم** |
| صباح اليوم التالي للموعد | «سارة — بوتوكس — أمس» | زر واحد: **حضرت** / **لم تحضر** |

- يستخدم عقد القناة الموجود؛ `quick_replies` موجود أصلاً في `OutgoingMessage`
- لا تطبيق جديد، لا تسجيل دخول، لا لوحة تحكم، لا إجراء يومي
- عدم الرد حالة صريحة: `LAPSED` أو «غير مثبت» — **وليس افتراض نجاح**
- كل إدخال يُسجَّل كحدث بهوية من أدخله ووقته → قابلية تدقيق كاملة

### شروط قبول قابلة للقياس (Gate B)

- زمن الإدخال لكل حجز ≤ **10 ثوانٍ**
- معدل الاستجابة للإشعارات ≥ **70%** خلال أسبوع تشغيل حقيقي
- إن لم يتحقق الشرطان → **المشكلة في التصميم لا في الموظفة**، ويُعاد التصميم قبل عبور Gate B

`TO VALIDATE`: هل تقبل الموظفة هذا أصلاً؟ سؤال إلزامي في Commercial Validation (§14).

---

## 12. Baseline — إلزامي قبل الـPilot

> لا يبدأ Pilot قبل جمع Baseline. رقم بلا خط أساس ادّعاء، لا نتيجة.

| المقياس | المصدر | الطريقة |
|---|---|---|
| عدد الاستفسارات شهرياً | سجل محادثات العيادة | عدّ عينة أسبوعين + إسقاط |
| توزيعها على الخدمات | نفس المصدر | تصنيف يدوي |
| متوسط زمن أول رد | نفس المصدر | فرق الطوابع الزمنية — أرخص مقياس وأقواه بيعياً |
| نسبة الاستفسارات → طلب حجز | المحادثات + دفتر المواعيد | ربط يدوي |
| الحجوزات المؤكدة / المكتملة | دفتر أو نظام المواعيد | عدّ مباشر |
| No-show Rate | نفس المصدر | مكتمل ÷ مؤكد |
| متوسط قيمة الحجز المكتمل | الفواتير | متوسط بسيط |
| المتابعة اليدوية الحالية | مقابلة الموظفة | وصفي: من، متى، كم مرة، بأي صيغة |
| ساعات العمل ونمط الرد | مقابلة | يحدد نافذة الصمت الواقعية |
| **الطاقة الاستيعابية المتاحة** | مقابلة | مواعيد شاغرة أسبوعياً — 🔴 انظر R1 |

**القواعد:** 4 أسابيع حد أدنى (أقل لا يلتقط التذبذب الأسبوعي) · لا يُفترض أي رقم · غير المتوفر يُكتب «غير متاح» مع أثره على ما يمكن إثباته · المخرَج صفحة واحدة موقّعة من العيادة قبل التشغيل، تكون المرجع الوحيد لأي نقاش نتائج لاحق.

**إن كانت بيانات العيادة فوضوية** (الوضع الأرجح): تُجمع يدوياً مع الموظفة. هذه ليست عقبة بل **أول قيمة نقدمها** — أول مرة ترى العيادة أرقامها.

---

## 13. النموذج التجاري

### المقارنة

| | Pure Revenue Share | **Fixed Monthly + Performance** |
|---|---|---|
| جاذبية البيع | عالية جداً | متوسطة |
| متطلب البيانات | وصول موثوق للفواتير والحضور | تأكيد الحضور للجزء المتغير فقط |
| خطر النزاع | مرتفع جداً | محصور في مكوّن واحد |
| التدفق النقدي | صفر حتى تتحقق النتائج | يغطي التكلفة من الشهر الأول |
| خطر التلاعب | تسجيل حضور ناقص لتقليل الفاتورة | ضعيف |
| ملاءمة الحالة الحالية | **مستحيل** — النظام لا يرى الحضور | ممكن فور Gate B |

### التوصية المعتمدة

**اشتراك شهري ثابت** مقابل: الرد الفوري، عدم ضياع أي استفسار، محرك المتابعة، تقرير القمع الشهري.
**+ رسم أداء** لكل Verified Recovered Completed Booking (§9.3): **مبلغ ثابت لكل حجز، لا نسبة**، بسقف شهري معلن.

**لماذا مبلغ ثابت لا نسبة:** النسبة تفتح النقاش على قيمة كل خدمة وخصوماتها وباقاتها، وتفرض وصولاً لبيانات مالية لن تمنحها عيادة في أول عقد. المبلغ الثابت يحوّل النقاش إلى سؤال واحد قابل للحسم: **هل حضرت هذه العميلة؟**

### السعر: `TO VALIDATE`

لا يُحدَّد قبل Baseline عيادتين على الأقل. المدخلات المطلوبة: متوسط قيمة الحجز · حجم الـUnbooked الشهري · Recovery Rate الواقعي · ما تعتبره العيادة سعراً معقولاً · تكلفة تشغيلنا لكل Lead (استدعاءات AI + رسائل القنوات).

### قواعد منع النزاع — تُكتب في العقد

1. **قائمة مسبقة لا لاحقة.** تُرسل أسبوعياً قائمة الـLeads التي تلقت متابعة، **قبل** معرفة من سيحضر. لا إسناد رجعي.
2. **الصمت = لا استحقاق.** لم تُؤكَّد الحضور خلال المهلة → لا تُحتسب. العبء علينا لا على العيادة.
3. **سقف شهري معلن** يمنع فاتورة مفاجئة تُنهي العلاقة.
4. **لا ادعاء سببية فردية** (بند §9.4 الحرفي).
5. **ملكية البيانات:** بيانات العميلات ملك العيادة، وتُسلَّم كاملة عند انتهاء العلاقة. `TO VALIDATE` مع مستشار قانوني.

**عند نقص البيانات:** ينهار المكوّن المتغير للصفر تلقائياً ويبقى الاشتراك. النموذج يتقلص ولا ينكسر. أي نموذج يعتمد كلياً على بيانات لا نتحكم بها هو نموذج هش.

---

## 14. Commercial Validation (مسار متوازٍ إلزامي) 🔴

> تحسين الكود إلى ما لا نهاية لا يثبت أن العيادة ستدفع.

يعمل بالتوازي مع Gate A، ويُكمَل **قبل** Gate D.

| السؤال | نوعه | حالته |
|---|---|---|
| كم استفساراً يصلكم شهرياً؟ | رقم | `TO VALIDATE` |
| كم منها ينتهي بحجز؟ | رقم | `TO VALIDATE` |
| ما نسبة عدم الحضور؟ | رقم | `TO VALIDATE` |
| متوسط قيمة الحجز؟ | رقم | `TO VALIDATE` |
| كيف تتابعون من لم يحجز اليوم؟ | وصفي | `TO VALIDATE` |
| هل «الاستفسارات الضائعة» مشكلة تشعرون بها؟ | نوعي — **الأهم** | `TO VALIDATE` |
| **هل لديكم مواعيد شاغرة أصلاً؟** | رقم — 🔴 R1 | `TO VALIDATE` |
| **هل تعطون السعر في المحادثة أم تدعون للحضور؟** | نوعي — 🔴 R2 | `TO VALIDATE` |
| هل تقبل الموظفة إدخال التأكيد والحضور؟ | نوعي | `TO VALIDATE` |
| هل تقبلون مجموعة قياس ضابطة؟ | نوعي | `TO VALIDATE` |
| هل يقبل النموذج: ثابت + أداء؟ | نوعي | `TO VALIDATE` |
| ما السعر الذي تعتبرونه منطقياً؟ | رقم | `TO VALIDATE` |

**العدد المطلوب:** 5 مقابلات كحد أدنى، عيادتان توقعان Baseline.
**معيار النجاح:** عيادة واحدة على الأقل تقبل الأسئلة الثلاثة الحرجة (Holdout، إدخال الحضور، نموذج التسعير). **رفضها الثلاثة يعني أن نموذج المنتج لا القياس هو ما يحتاج تغييراً.**

---

## 15. محركات النمو وترتيبها

| المحرك | أثر الإيراد | سهولة الإثبات | التكلفة | الحالة |
|---|---|---|---|---|
| 1. Lead Conversion | عالٍ | متوسطة | منخفضة (مبني) | يحتاج F1/F5/F6 |
| 2. Lead Recovery | متوسط–عالٍ | **عالية** | منخفضة (مبني) | يحتاج F1 + Holdout |
| **3. No-show Reduction** | متوسط–عالٍ | **الأعلى** | **الأدنى** | غير مبني — **التالي** |
| 4. Retention / إعادة تنشيط | عالٍ نظرياً | منخفضة | مرتفعة | مؤجَّل بقرار صريح |

**No-show Reduction هو التالي بعد Gate A/B.** ثلاثة أسباب، الثالث حاسم:

1. أرخص تنفيذ في المشروع (تذكير مجدول، بلا فهم لغوي)
2. أسهل إثبات (رقم واحد قبل/بعد، وقابل لنفس آلية الـHoldout)
3. **تنفيذه يفرض بناء حلقة رجوع الحضور** — وهي البيانات التي بدونها لا يعمل أي شيء آخر. ليس محركاً إضافياً بل الطريق الأرخص لما يحتاجه كل شيء

**Retention مؤجَّل:** يحتاج سجل عملاء رقمياً (نادر) + موافقات تسويقية + يقترب من بيانات صحية — ثلاثة مخاطر مقابل إثبات ضعيف.

---

## 16. Response Evolution

> **النظام يتعلّم أي تدخّل معتمد يُنتج نتيجة تجارية أفضل — لا كيف يتكلم بشكل أجمل.**

| المرحلة | المضمون | المتطلب | Gate |
|---|---|---|---|
| 1. Track | كل رسالة صادرة تحمل `variant_id` وتُسجَّل | العناصر الأربعة (D3) | A |
| 2. Measure | ربط كل Variant بنتيجته النهائية (Request → Confirmed → Completed) | حلقة الرجوع | B |
| 3. Compare | تقرير أداء كل Variant مع حجم العينة ومجال الثقة **إلزامياً** | بيانات Pilot | D |
| 4. Human-approved | **إنسان** يقرر إيقاف أو ترجيح Variant | قرار موثّق | D |
| 5. Controlled experimentation | توزيع شبه آلي | إثبات أن المرحلة 4 أنتجت قيمة | E |

### الحدود الثابتة

```
يتطور:      الصياغة · الـCTA · توقيت المتابعة · عدد المتابعات · اختيار الـVariant
لا يتطور:   السعر · مدة العلاج · أي ادّعاء طبي · سياسات العيادة · أي التزام تعاقدي
```

كل Variant نص معتمد بشرياً مسبقاً في مكتبة. لا توليد حر · لا ML · لا Bandits · لا Fine-tuning · لا Agent بصلاحيات.

### القيد الإحصائي (يُقال بصراحة)

بعيادة واحدة وبضع مئات من الـLeads شهرياً، الفرق بين صياغتين لن يكون ذا دلالة لأشهر. لذلك:

- **حد أقصى 2 Variant لكل نية.** ثلاثة تقسّم عينة صغيرة أصلاً
- **يُمنع اتخاذ قرار على فرق غير دال** — التقرير يعرض حجم العينة ومجال الثقة إلزامياً
- **الأثر الأكبر في التوقيت والعدد، لا الصياغة.** فارق المتابعة بعد 6 ساعات مقابل 24 أكبر بكثير من فارق كلمتين — تُختبر هذه أولاً

---

## 17. حدود الـAI (قابلة للاختبار)

**قاعدة أساسية: النظام يعمل بشكل صحيح وكامل عند `USE_AI_INTENT=false`.** هذا ليس وضع طوارئ بل الوضع المرجعي.

| يُسمح لـAI | لا يُسمح لـAI |
|---|---|
| تصنيف النية ضمن قائمة مغلقة | تحديد أو ذكر أي سعر |
| فهم اللهجة العراقية | تغيير حالة جلسة أو حجز |
| كشف الغموض بين الخدمات | كتابة أو تعديل نص يُرسل |
| — | تسجيل أو تعديل Lead |
| — | تقرير أهلية متابعة أو حساب إيراد |

**تأكيدات قابلة للاختبار (تُكتب كاختبارات في Gate A):**

1. مع `USE_AI_INTENT=false` لا يقع أي اتصال شبكي خارجي، والقمع الكامل يعمل
2. لا يوجد مسار تنفيذ يجعل مخرَج AI مصدراً لسعر
3. مخرَج AI خارج القائمة المغلقة → `fallback` آمن، ولا يوقف الرد
4. تبديل مزود AI لا يتطلب أي تعديل في `business_logic.py`

---

## 18. Safety Rails وشروط دخول الـPilot

| # | القاعدة | الحالة | التصنيف |
|---|---|---|---|
| S1 | لا سعر إلا من `clinic_config` — لا اختراع ولا تقريب | ✅ قائمة | — |
| S2 | خدمة غامضة → استيضاح، لا تخمين | ❌ مكسورة (F6) | 🔴 Pilot Blocker |
| S3 | لا معلومة طبية ولا وعد بنتيجة | ✅ قائمة | — |
| S4 | AI يفهم ولا يقرر؛ قائمة مغلقة؛ Fallback آمن | ✅ قائمة | — |
| S5 | كل رد نص معتمد بشرياً | ✅ فعلياً | — |
| S6 | `automation_paused` لكل Lead | ❌ غير موجود | 🔴 Pilot Blocker |
| S7 | حد أقصى صارم لعدد الرسائل لكل Lead | ⚠️ ضمني، غير معلن | 🔴 Pilot Blocker |
| S8 | ساعات إرسال آمنة | ❌ غير موجود | 🔴 Pilot Blocker |
| S9 | Idempotency للرسائل الواردة | ✅ قائمة | — |
| S10 | منع تكرار المتابعة عند فشل جزئي | ⚠️ يحتاج تحقق الـAudit | 🔴 إن ثبت الخلل |
| S11 | التعافي من فشل منتصف الدورة | ⚠️ يحتاج تحقق الـAudit | 🟡 |
| S12 | حماية البيانات الشخصية (تخزين وسجلات وطرفية) | ⚠️ الهواتف تُطبع في الطرفية | 🔴 Pilot Blocker |
| S13 | قابلية التدقيق — كل رسالة صادرة لها أثر | ❌ غير موجود (F4) | 🔴 Pilot Blocker |

🔴 **لا يمر Gate B وأي بند 🔴 مفتوح.** لا استثناءات، ولا «نصلحه أثناء الـPilot».

---

## 19. قيود القنوات (تُصمَّم لها، لا تُبنى)

WhatsApp خارج النطاق الحالي. قيوده تعيد تشكيل تصميم المتابعة ويجب استيعابها قبل تثبيت أي منطق:

| القيد | الأثر التصميمي |
|---|---|
| نافذة خدمة 24 ساعة من آخر رسالة من العميلة؛ خارجها لا نص حر | متابعتنا عند 24 ساعة تقع على الحافة تماماً. **6–12 ساعة تبقى داخل النافذة** — أرخص، أبسط، وعلى الأرجح أفضل تجارياً |
| خارج النافذة: قوالب معتمدة من Meta فقط | Variants المتابعة تصبح قوالب معتمدة. **المرحلة 5 شبه مستحيلة على القوالب الخارجة عن النافذة** |
| لغة إعادة التفعيل تُصنَّف تسويقية والقوالب التسويقية مدفوعة | لكل متابعة تكلفة نقدية → **اقتصاديات المتابعة تُحسب لكل Lead** |
| يشترط Opt-in مسبق | 🔴 **حقل موافقة في الـDomain Model من الآن** — رخيص الآن، مستحيل بأثر رجعي |
| Instagram: نوافذ وسياسات مختلفة | لا افتراضات خاصة بقناة داخل منطق المتابعة |

**القرار التصميمي:** التوقيت والقالب وقدرة القناة على حمل نص حر → **معطيات لكل قناة**، لا ثوابت في الكود (`FIRST_FOLLOWUP_HOURS = 24` ثابت حالياً في `send_followups.py`).

**القيد المضاد:** لا يُبنى أي كود خاص بواتساب الآن. تُستوعب القيود في شكل المعطيات فقط.

⚠️ سياسات وتسعير Meta تتغير باستمرار — تُتحقَّق من التوثيق الرسمي قبل أي التزام تعاقدي أو حساب اقتصاديات.

---

## 20. الـGates

> الانتقال بشرط **تجاري أو دلالي**، لا باكتمال الكود.

### Gate A — سلامة الأساس

| | |
|---|---|
| **الهدف** | كل Lead مرئي، كل مصطلح صادق، كل رسالة متتبَّعة |
| **المدخلات** | تقرير الـAudit معتمَداً |
| **التنفيذ** | إصلاح F1–F8 وS2 بأقل تغيير. لا ميزات جديدة إطلاقاً |
| **شروط الخروج** | كل Lead يسأل عن سعر له سجل بـ`lead_id` مستقر · لا مصطلح في الكود يخالف §8 · كل رسالة صادرة عبر مسار واحد تنتج حدثاً بـ`variant_id` · الخدمة الغامضة تنتج سؤالاً لا سعراً · اختبارات تغطي دورة حياة الـLead وتأكيدات §17 |
| **الدليل** | سيناريو الـLead الصامت يُنتج سجلاً ومتابعة · تقرير قمع مشتق من `events.jsonl` وحده · اختبارات خضراء |

### Gate B — نظام قابل للقياس وآمن

| | |
|---|---|
| **الهدف** | إغلاق القمع حتى الحضور، وإغلاق كل ثغرة أمان تشغيلي |
| **المدخلات** | Gate A + قرار Q2 (آلية الإدخال) |
| **التنفيذ** | Clinic Feedback Loop (§11) + كل بند 🔴 في §18 |
| **شروط الخروج** | القمع كاملاً حتى Completed مشتق من `events.jsonl` · **صفر بنود 🔴 مفتوحة** · إدخال الحضور ≤10 ثوانٍ · إيقاف الأتمتة لعميلة خلال ثوانٍ |
| **الدليل** | أسبوع تشغيل داخلي: معدل استجابة الإشعارات ≥70% · لا رسالة خارج الساعات الآمنة · لا متابعة مكررة |

### Gate C — جاهزية الـPilot

| | |
|---|---|
| **الهدف** | لا يبدأ Pilot إلا وهو قابل للإثبات |
| **المدخلات** | Gate B + Baseline 4 أسابيع + مخرجات Commercial Validation |
| **التنفيذ** | تحديد `holdout_percentage` من الـBaseline وحساب MDE + عقد يتضمن قواعد §13 |
| **شروط الخروج** | Baseline موقّع · نسبة الـHoldout محسوبة لا مفترضة · العيادة قبلت المجموعة الضابطة كتابةً · **حساب MDE يثبت أن الأثر المتوقع قابل للاكتشاف خلال مدة الـPilot** |
| **الدليل** | وثيقة Baseline · حساب MDE مكتوب · عقد موقّع |

### Gate D — إثبات القيمة

| | |
|---|---|
| **الهدف** | رقم واحد قابل للدفاع عنه |
| **المدخلات** | Gate C |
| **التنفيذ** | تشغيل Pilot + مراحل Response Evolution 3 و4 |
| **شروط الخروج** | تقرير Incremental Revenue بمجال ثقة، **أو إثبات أن الأثر صفر** — وكلاهما نتيجة مقبولة توقف حرق الوقت · قرار Variant بشري واحد موثّق |
| **الدليل** | التقرير · قبول العيادة لأرقامه · أول فاتورة أداء مدفوعة أو مبرَّر عدمها |

### Gate E — التوسع

قناة ثانية · No-show Engine · تخزين أقوى · المرحلة 5. **لا يبدأ قبل عبور Gate D.**

---

## 21. مؤجَّل بقرار صريح

PostgreSQL · WhatsApp · Instagram · n8n · Multi-tenancy · ML / Fine-tuning · Bandits · AI Agent حر · Dashboard · Booking Calendar · Multi-touch Attribution · Retention Engine · Identity Resolution · Personalization · تطبيق للموظفة.

**الاستثناء الوحيد:** يُستأنف النقاش فقط إذا أثبت الـAudit بدليل من الكود أن البند **يمنع** Gate A أو B — لا لأنه أفضل ممارسة.

---

## 22. سجل المخاطر

> «لو استثمرت سنة في هذا المنتج، ما الذي قد يُفشلني رغم أن الهندسة ممتازة؟»

| # | المخاطرة | النوع | لماذا قاتلة | العلاج |
|---|---|---|---|---|
| **R1** 🔴 | **الطاقة الاستيعابية.** لو كانت مواعيد العيادة ممتلئة، فالحجوزات الإضافية تزاحم أخرى ولا تنتج ديناراً إضافياً | تجارية | تجعل الإيراد الإضافي **صفراً هيكلياً** مهما كان المنتج ممتازاً | سؤال تأهيل إلزامي في §14 · عيادة بلا مواعيد شاغرة **تُستبعد من الـPilot** |
| **R2** 🔴 | **استراتيجية السعر.** كثير من عيادات التجميل تتعمد **عدم** ذكر السعر في المحادثة لجذب العميلة للحضور. أتمتة ذكر السعر قد **تخفض** التحويل | منتج | تنسف الافتراض الأساسي للـVertical Slice كله | سؤال إلزامي في §14 · إن ثبت، يتحول الرد الافتراضي من «سعر» إلى «حجز استشارة» — **والمعمارية تحتمله لأن الرد Variant** |
| **R3** 🔴 | **تبنّي الموظفة.** المستخدم الحقيقي ليس المشتري وليس لديه حافز | تشغيلية | بلا إدخال الحضور ينهار القياس والفوترة معاً | §11 بشرطي قبول قابلين للقياس · حافز صغير للموظفة يُناقَش مع العيادة |
| **R4** 🔴 | **حجم العينة.** عيادة واحدة قد لا تنتج دلالة إحصائية أبداً | قياس | نموذج «أثبت بالأرقام» ينهار بلا قدرة إحصائية | حساب MDE شرط خروج من Gate C · التوجه لعيادتين لا واحدة |
| **R5** 🟡 | **الموسمية والحملات.** طفرة إعلانية أثناء الـPilot تلوّث المقارنة | قياس | تحوّل نجاحاً وهمياً إلى وعد لا يتكرر | مجموعة ضابطة **متزامنة** بدل مقارنة قبل/بعد — وهذا سببها الأول |
| **R6** 🟡 | **اقتصاديات الوحدة.** تكلفة AI + رسائل القنوات لكل Lead قد تلتهم الاشتراك | تجارية | نمو يزيد الخسارة | قياس التكلفة لكل Lead في Gate B قبل التسعير |
| **R7** 🟡 | **تنظيمي.** قواعد الإعلان والتواصل التسويقي للخدمات الطبية والتجميلية تختلف بالسوق | قانونية | إيقاف مفاجئ للنشاط | `TO VALIDATE` مع مستشار محلي قبل Gate C |
| **R8** 🟡 | **اعتماد على قناة واحدة.** Telegram ليس قناة عملاء عيادات التجميل فعلياً | منتج | Pilot غير واقعي | خيار **Concierge Pilot**: النظام يقترح والموظفة ترسل من قناة العيادة — يثبت القيمة بلا أي تكامل |
| **R9** 🟡 | **ملكية البيانات والاحتفاظ** | قانونية/تجارية | نزاع عند انتهاء العلاقة | بند صريح في العقد (§13) |
| **R10** 🟢 | **تركّز الاعتماد على مؤسس واحد** | تشغيلية | خمسة Gates وقت طويل | تسلسل Gates يسمح بالتوقف عند أي نقطة بنتيجة معروفة |

**R1 وR2 أخطر ما في هذه الوثيقة**، لأن كليهما يمكن أن يبطل المنتج بالكامل، ولا يمكن اكتشاف أي منهما من الكود — فقط من مكالمة مع عيادة. لهذا §14 ليس اختيارياً.

---

## 23. القرارات المفتوحة

| # | القرار | التوصية المبدئية | يُحسم في |
|---|---|---|---|
| Q1 | نافذة الصمت قبل `UNBOOKED` | 6–12 ساعة لا 24 — تبقى داخل نافذة واتساب وتوافق سلوك العميلات | Gate C من الـBaseline |
| Q2 | آلية إدخال التأكيد والحضور | إشعار بأزرار في محادثة الطاقم (§11) | Gate B بعد ملاحظة سير العمل |
| Q3 | `holdout_percentage` | يُحسب من MDE (§10) | Gate C |
| Q4 | مهلة التأكيد قبل `LAPSED` | 48 ساعة | Gate B |
| Q5 | سعر الاشتراك ورسم الأداء | بعد Baseline عيادتين | Gate C |
| Q6 | هل الرد الافتراضي سعر أم دعوة استشارة؟ | يُحسم من R2 | Commercial Validation |

---

## 24. طريقة العمل مع Claude Code

```
PRD.md (هذه الوثيقة) → AGENTS.md → الكود → Claude Code
```

**الدورة:** Audit → مراجعتنا → قرارات معمارية → مهمة صغيرة واحدة → اختبار → مراجعة → Commit → التالية.

**القواعد:**
- لا مهمة تُغلق بلا اختبار
- فرع لكل مهمة، لا Commit مباشر على `main`
- أي قرار معماري يُسجَّل في `.memory/decisions.md`
- 🔴 = يمنع Gate A أو B. أي شيء آخر 🟡 أو 🟢 مهما كانت لهجة صياغته
- عند تعارض الكود مع هذه الوثيقة: يُصحَّح أحدهما صراحةً ويُسجَّل، ولا يُتجاهل

**تحديث مطلوب:** `AGENTS.md` يصف المشروع حالياً بأنه «نظام مساعد محادثة» — تموضع v1. يجب تحديثه ليعكس هذه الوثيقة.

---

## 25. التقييم النهائي

| البُعد | الدرجة | الدليل | المخاطرة المتبقية | الإجراء |
|---|---|---|---|---|
| Problem | 9 | المشكلة محددة بقمع ونقاط تسرب ملموسة | لم تُؤكَّد ميدانياً كأولوية أولى للعيادة (R2) | §14 — 5 مقابلات |
| Revenue Model | 10 | 4 طبقات إيراد، مصفوفة ملكية، الإيراد = الحضور فقط | R1 قد يصفّر الأثر | R1 سؤال تأهيل مانع |
| Measurement | 10 | كل تقرير من `events.jsonl`؛ نقاط التسرب موصوفة وموسومة | جودة الإدخال البشري | §11 بشرطي قبول |
| Incrementality | 9 | Holdout حتمي، نسبة محسوبة لا مفترضة، MDE شرط خروج | R4: قد لا تكفي عينة عيادة واحدة أبداً | Gate C يكشفها قبل الصرف |
| Safety | 10 | 13 حاجزاً مصنّفة، 7 منها Pilot Blockers، حدود AI اختبارية | لا شيء غير مغطى وثائقياً | Gate B صفر بنود 🔴 |
| Product Positioning | 10 | «ليس» صريحة، اختبار قرار واحد، نص العقد يمنع ادعاء السببية | انزلاق تدريجي أثناء التنفيذ | اختبار §1 في كل مراجعة مهمة |
| Architecture | 10 | Domain Model أدنى، أحداث بالإلحاق، قيود القنوات معطيات | — | الـAudit يؤكد الكلفة |
| Pilot Readiness | 6 | الشروط معرّفة بالكامل | 7 بنود 🔴 مفتوحة + لا Baseline | Gates A→B→C |
| Commercial Readiness | 5 | النموذج والقواعد وبنود النزاع جاهزة | **صفر مقابلات، صفر أرقام حقيقية، السعر مجهول** | §14 — يبدأ اليوم |
| Claude Code Readiness | 10 | Gates بشروط خروج، قرارات محسومة، تصنيف صارم، برومبت Audit جاهز | — | — |

### النتيجة

```
PRD كوثيقة قرار:        9.9 / 10
جاهزية المنتج الفعلية:   8.0 / 10
```

**لماذا لا أعطي 10/10 للمنتج، ولن أفعل:**

الأبعاد الثلاثة الناقصة (Problem 9، Pilot 6، Commercial 5) **لا تُرفع بالكتابة، بأي جودة كتابة**. رفعها بقلم يعني تزوير حالة المشروع. كل منها الآن **محتوى داخل Gate بدليل مطلوب محدد**:

| ما يمنع 10 | قابل للإصلاح الآن؟ | الـGate الحاوي | الدليل المطلوب |
|---|---|---|---|
| المشكلة غير مؤكدة ميدانياً | لا — يحتاج مقابلات | Commercial Validation §14 | 5 مقابلات، منها 3 تؤكد أن ضياع الاستفسارات مشكلة محسوسة |
| 7 بنود Safety 🔴 مفتوحة | نعم — كود | Gate B | صفر بنود 🔴 + أسبوع تشغيل نظيف |
| لا Baseline ولا أرقام حقيقية | لا — يحتاج 4 أسابيع | Gate C | وثيقة موقّعة من عيادتين |
| السعر غير محقَّق | لا — قرار تجاري بعد بيانات | Gate C | عيادة تقبل النموذج والسعر |
| R1/R2 قد يبطلان المنتج | لا — يحتاج مكالمات | Commercial Validation §14 | مواعيد شاغرة موثّقة + سياسة تسعير المحادثة |
| R4 قد يمنع الدلالة الإحصائية | جزئياً — يُكشف بالحساب | Gate C | حساب MDE مكتوب |

**النتيجة الوحيدة الصادقة: 8.0/10 هي الدرجة الصحيحة لمشروع في هذه المرحلة، وارتفاعها يأتي من الميدان لا من المحرر.** أي وثيقة تعطي نفسها 10/10 قبل مكالمة واحدة مع عيادة تصف أمنية لا منتجاً.

**الخطوة التالية:** `AGENTS.md` → `CLAUDE.md` → Architecture Audit → مراجعة التقرير → Gate A — بالتوازي مع بدء §14 اليوم.
