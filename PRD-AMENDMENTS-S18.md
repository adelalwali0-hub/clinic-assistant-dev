# تعديل §18 — الحواجز في وضع Concierge

> **السبب:** §18 كُتب لنظام يرسل بنفسه على تلغرام. الملحق v3.1 أثبت أن شكل الـPilot المعتمد هو **Concierge**: النظام يقترح، ومشغّل بشري يرسل من حساب العيادة على انستغرام.
> **الأثر:** ثلاثة حواجز مفتوحة لن تُختبر في الـPilot الأول، وحاجز واحد مفقود تماماً وهو الأخطر تجارياً.
> **الطريقة:** إعادة تصنيف لا إسقاط. لا حاجز يُحذف؛ يُعاد ترتيب متى يُختبر ومتى يمنع بوابة.

---

## 1. المبدأ الحاكم

في Concierge، **من يضغط «إرسال» إنسان**. هذا ينقل ملكية ثلاثة حواجز من النظام إلى المشغّل:

| الحاجز | من يملكه في وضع مباشر | من يملكه في Concierge |
|---|---|---|
| S6 إيقاف الأتمتة | النظام | المشغّل — يتوقف عن الإرسال |
| S7 حد الرسائل | النظام | المشغّل — يرى ما أرسل |
| S8 ساعات آمنة | النظام | المشغّل — يرسل حين يعمل |

**لا يعني هذا أن الحواجز غير ضرورية.** يعني أنها **لا تُختبر** في الـPilot الأول، وأن اختبار Gate B لها سيكون فارغاً: «لا رسالة خارج الساعات الآمنة» صحيحة تلقائياً حين لا يرسل النظام شيئاً.

🔴 **وتصبح إلزامية لحظة أول رسالة آلية** — أي عند التكامل مع انستغرام (بعد App Review) أو العودة إلى قناة مباشرة.

---

## 2. الحاجز المفقود — S14

§18 لا يحمل حاجزاً لأخطر ثغرة في وضع Concierge، وهي مسجّلة كتسرّب **L6** في الملحق v3.1 §2:

> **الرسالة المقترحة قد لا تُرسل إطلاقاً. لا شيء في النظام يثبت خروجها.**

يُضاف إلى §18:

| # | القاعدة | الحالة | التصنيف |
|---|---|---|---|
| **S14** | **إثبات الإرسال الفعلي في وضع Concierge.** لا يُكتب `FOLLOWUP_SENT` ولا `RESPONSE_SENT` إلا لحظة تأكيد المشغّل أن الرسالة غادرت. الاقتراح غير المُرسَل لا يُنتج حدثاً ولا استحقاقاً | ❌ غير موجود | 🔴 **Pilot Blocker** |

**لماذا 🔴:** §9.3 يشترط `FOLLOWUP_SENT` موثّقاً بحدث كشرط ثالث للفوترة. بلا إثبات الإرسال تنهار وحدة الفوترة **قبل** أن تصل إلى الحضور — أي أن L6 يسبق L3 في الخطورة.

**الحالة في الكود اليوم:** `outbound.send()` يُصدر الحدث عند نجاح `channel.send_message` — وهو صحيح لوضع مباشر، وغير موجود أصلاً لوضع Concierge (لا مسار إدخال بشري).

---

## 3. التصنيف المعدَّل للحواجز الثلاثة عشر

| # | القاعدة | الحالة | Concierge | مباشر |
|---|---|---|---|---|
| S1 | لا سعر إلا من `clinic_config` | ✅ | 🔴 يُختبر | 🔴 |
| S2 | خدمة غامضة → استيضاح | ✅ **أُغلق (Gate A)** | 🔴 يُختبر | 🔴 |
| S3 | لا معلومة طبية ولا وعد | ✅ | 🔴 يُختبر | 🔴 |
| S4 | AI يفهم ولا يقرر | ✅ **مُختبَر (§17)** | 🔴 يُختبر | 🔴 |
| S5 | كل رد نص معتمد بشرياً | ✅ **مُقوّى (`variants.py`)** | 🔴 يُختبر | 🔴 |
| S6 | `automation_paused` لكل Lead | ❌ | 🟡 **مؤجَّل** — لا أتمتة تُوقَف | 🔴 |
| S7 | حد أقصى لعدد الرسائل | ❌ | 🟡 **مؤجَّل** — المشغّل يرى ويحدّ | 🔴 |
| S8 | ساعات إرسال آمنة | ❌ | 🟡 **مؤجَّل** — المشغّل يرسل حين يعمل | 🔴 |
| S9 | Idempotency للوارد | ⚠️ **هشّ** — `set()` في الذاكرة يموت مع العملية | 🟡 | 🔴 |
| S10 | منع تكرار المتابعة | ✅ **مُختبَر (Gate A)** | 🔴 يُختبر | 🔴 |
| S11 | التعافي من فشل منتصف الدورة | ✅ **عولج (Gate A)** | 🟡 | 🟡 |
| S12 | حماية البيانات الشخصية | ❌ **الهواتف تُطبع في الطرفية** | 🔴 يُختبر | 🔴 |
| S13 | قابلية التدقيق | ✅ **أُغلق (Gate A)** | 🔴 يُختبر | 🔴 |
| **S14** | **إثبات الإرسال الفعلي** | ❌ | 🔴 **يُختبر** | 🟢 لا ينطبق |

### تصحيح على S9

الوثيقة تضع ✅ صارمة. الواقع: `_processed_message_ids` مجموعة في الذاكرة (`message_router.py:55`) تُفقد عند إعادة التشغيل، فرسالة قد تُعالَج مرتين بعد انقطاع.

**التصنيف الصحيح: ⚠️ قائم لكنه هشّ.** ولا يُترك ✅.

---

## 4. ما يمنع Gate B فعلاً — في وضع Concierge

**قبل هذه المراجعة:** سبعة بنود 🔴 مفتوحة (S2, S6, S7, S8, S10, S12, S13).

**بعدها:**

| مغلق في Gate A | S2 · S10 · S13 |
|---|---|
| **مفتوح ويمنع Gate B** | **S12** (حماية البيانات) · **S14** (إثبات الإرسال) |
| مؤجَّل حتى أول رسالة آلية | S6 · S7 · S8 |

**بندان لا خمسة.** وهذا يقصّر الطريق إلى Gate B جوهرياً.

⚠️ **الشرط الصريح:** لحظة إرسال النظام رسالة واحدة تلقائياً، تعود S6 وS7 وS8 إلى 🔴 فوراً، ولا يمر Gate B بأي منها مفتوحاً. التأجيل مشروط بشكل الـPilot لا بالوقت.

---

## 5. أثر ذلك على ترتيب العمل

| # | العمل | يمنع؟ |
|---|---|---|
| ١ | معطيات `config` (§19، Q1) | لا — لكنه يجعل S7/S8 أرخص لاحقاً |
| ٢ | حقل الموافقة (§19) | لا — لكن كلفته ترتفع كل يوم تشغيل |
| ٣ | **S12** حماية البيانات | 🔴 **نعم** |
| ٤ | **S14** إثبات الإرسال | 🔴 **نعم** |
| ٥ | حلقة الرجوع (§11) | 🔴 نعم — **لكنها تحتاج عيادة موافقة** |

**الثلاثة الأولى تُبنى بلا عيادة.** الرابع يحتاج قراراً على شكل تأكيد الإرسال. والخامس يحتاج ميداناً.

---

## 6. ما لم يتغيّر

- لا حاجز حُذف. الثلاثة عشر قائمة، وأُضيف رابع عشر
- 🔴 **لا يمر Gate B وأي بند 🔴 مفتوح** — القاعدة كما هي
- التأجيل **مشروط بوضع Concierge** ويسقط بأول رسالة آلية
- S12 لم يخفّ: طباعة أرقام الهواتف في الطرفية خطر في كل وضع

---

## 7. القرار المطلوب

**S14 يحتاج قراراً تصميمياً قبل تنفيذه:** كيف يؤكد المشغّل أن الرسالة أُرسلت؟

| الخيار | الكلفة | الدقة |
|---|---|---|
| أمر في الطرفية بعد كل إرسال | منخفضة | يعتمد على انضباط المشغّل |
| ملف يومي يُراجَع ويُعلَّم | منخفضة | تأخير في التسجيل |
| تكامل انستغرام | عالية جداً | تلقائي |

القرار مؤجَّل حتى تثبيت شكل تشغيل الـPilot.
